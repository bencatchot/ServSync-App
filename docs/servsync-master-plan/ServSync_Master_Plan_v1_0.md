<!-- Converted from docs/servsync-master-plan/ServSync_Master_Plan_v1_0_Updated.docx. -->
<!-- Keep this Markdown companion synchronized with roadmap/product-direction changes. -->

# ServSync Master Plan

**v1.0 Updated Draft**

Product strategy, roadmap, marketplace positioning, beta plan, and Codex-ready operating reference

| Purpose<br>This document is the execution planning reference for ServSync. It captures current build understanding, strategic decisions from prior chats, open gaps, implementation priorities, rollout state, and the near-term execution plan. It is intended to live in GitHub as a reference document for the founder, ChatGPT, Codex, and future contributors. |
| --- |

Prepared for Ben Catchot | June 15, 2026

# Documentation Hierarchy

The [ServSync Product Vision & Philosophy](ServSync_Product_Vision_and_Philosophy_v1.md) is the authoritative source for product purpose, target users, guiding principles, and long-term direction. The [ServSync Product Information Architecture](ServSync_Product_Information_Architecture_v1.md) translates that vision into platform structure, module ownership, navigation principles, cross-profile workflow boundaries, and expansion rules. The [ServSync Contractor Work Module Product Specification](ServSync_Contractor_Work_Module_Product_Specification_v1.md) defines the target contractor Work experience beneath that architecture. This Master Plan translates those documents into milestones, implementation priorities, rollout state, and future work.

Future module specifications, backlog priorities, roadmap decisions, and implementation plans must align with the Product Vision & Philosophy and Product Information Architecture.

Current documentation hierarchy:

1. [Product Vision & Philosophy](ServSync_Product_Vision_and_Philosophy_v1.md)
2. [Product Information Architecture](ServSync_Product_Information_Architecture_v1.md)
3. Module specifications
   - [Contractor Work Module Product Specification](ServSync_Contractor_Work_Module_Product_Specification_v1.md)
4. Master Plan
5. Backlog
6. Changelog

# Version Control and How to Use This Document

| Field | Value |
| --- | --- |
| Document name | ServSync Master Plan v1.0 Updated Draft |
| Recommended GitHub path | docs/strategy/ServSync_Master_Plan_v1_0.docx |
| Recommended editable source path | docs/strategy/servsync-master-plan-v1.md or docs/strategy/servsync-master-plan-v1-notes.md |
| Primary purpose | Execution source of truth for ServSync architecture, roadmap state, implementation priorities, rollout state, and future work |
| Update method | Update this document whenever major product, pricing, roadmap, beta, or messaging decisions change |
| Important guardrail | Do not let Codex make product changes from this document without a separate, narrow implementation prompt and explicit approval |

Recommended workflow: keep this Word document for human reading and review. If Codex needs to reference it, also store a Markdown version or summary in the repository because Codex/code agents usually work best with text-based files. The Word document can still live in GitHub, but a Markdown companion is more agent-friendly.

- Human-readable master file: docs/strategy/ServSync_Master_Plan_v1_0.docx
- AI/code-agent companion file: docs/strategy/servsync-master-plan-v1.md
- Roadmap implementation prompts should live separately under docs/prompts/ or docs/implementation-plans/.
- Never ask Codex to “do the whole master plan.” Ask Codex to audit or implement one clearly bounded section at a time.

# 1. Executive Summary

ServSync is a homeowner-contractor marketplace with contractor software. It is designed to help homeowners find local service contractors, connect with them in a controlled way, communicate clearly, request work, review estimates and invoices, preserve a Home History for their property, and create manual follow-up reminders. On the contractor side, ServSync gives solo and small-team contractors practical software tools for requests, estimates, jobs, invoices, reports, calendar events, customer/home records, and workflow follow-up.

The product should not be positioned as only contractor software or only a lead marketplace. Its strategic difference is the connection between the two: discovery plus the workflow that happens after discovery. The strongest simple platform message remains: Find local contractors. Keep the work organized.

The first commercial focus should stay on solo and small contractors. These users need affordable, practical tools and exposure to local homeowners, but often do not want the complexity or price of major field service management platforms. Homeowners should remain free in the early stages because homeowner adoption creates marketplace value for contractors.

| Current strategic decision<br>The primary button on a contractor profile should be Connect. This is more differentiated than a generic Message or Request Service button because the ServSync connection creates a permission-based working relationship, not just a one-off contact. |
| --- |

| Strategic area | Decision |
| --- | --- |
| Product category | Homeowner-contractor marketplace with contractor software |
| Primary paying customer | Solo and small contractors |
| Homeowner pricing | Free for core homeowner use during early beta and launch |
| Marketplace differentiator | Discover exposure plus connection-based workflow |
| Contractor profile CTA | Connect |
| Connection request | Should include message, selected property context, and permission selection; photos/media are a later phase after storage/RLS design |
| Messaging direction | Homeowner should be able to communicate with a contractor without creating a service request |
| Beta pricing direction | 30-day free trial, likely $20/mo premium after beta, possible $5/mo basic Discover/communication tier |
| Most evolving workflow | Job workflow based on beta user feedback |

FB-031 contractor beta billing-readiness adds a database-only foundation for tracking contractor beta/free/founder/future billing entitlement state separately from public contractor profile data. Existing beta contractors remain free with full beta access by default, homeowners remain outside contractor billing, and Stripe checkout, payment collection, credit-card requirements, UI enforcement, and live subscription activation remain future work until explicitly approved. Platform-admin billing-readiness visibility is internal and read-only: it shows canonical billing account status, legacy contractor profile subscription fields, Stripe presence booleans, and entitlement summaries without exposing full provider IDs or adding editable billing controls. The contractor dashboard now loads the centralized entitlement state and shows non-blocking beta/status labels for future subscription readiness. It also has limited UI support for future read-only states on representative creation/send actions, but normal beta/free/full-beta access remains unrestricted, existing records stay visible, and backend/RPC enforcement remains a future separately approved slice.

FB-032 Service Plans Foundation v1 (internally `service_agreement` records) is complete as a contractor maintenance-plan foundation: Slice 2 merged, applied, and verified the SQL/RLS/RPC foundation for contractor-owned templates, property-scoped offers to connected homeowners, homeowner accept/decline, and accepted plan snapshots; Slice 3A adds contractor-side template and offer UI for authorized contractor owner/admin/office users only; Slice 4A adds homeowner-side offer review, accept/decline through the existing response RPC, and read-only active plan cards inside the homeowner Estimates / Invoices area. Service Plans remain separate from requests, one-time Draft outputs, estimates, jobs, invoices, scheduling, reminders, notifications, payments, renewals, e-signatures, and external delivery until later explicit slices are approved.

FB-026 now includes a contractor-to-contractor referral path as a distinct trust/growth track. The production-applied SQL/RLS/RPC foundation stores `contractor_refers_contractor` referral invite leads submitted by contractor owner/admin/office users and reviewed by platform admins. The first contractor-facing slice adds only a compact submit UI in the contractor `Invites & Referrals` area so eligible contractors can send another contractor's details to the ServSync team for follow-up. The first platform-admin slice adds manual tracking inside the existing admin `Referrals` tab using only the existing admin list/update RPCs for status, notes, and outreach-date/count tracking. This remains intentionally separate from homeowner contractor invite leads, contractor-to-homeowner invites, reward referral codes, contractor team invites, connection requests, service requests, email delivery, rewards, billing, paywalls, contractor permissions, contractor team access, public referral links, account/profile creation, and automatic contractor profile creation. Email outreach, public landing pages, rewards, permissions/team access, account/profile creation, and any signup attribution beyond nullable admin match fields remain future slices requiring separate approval.

The official feature backlog has been structurally reconciled through PR #362 / main SHA `b23e18708601c515ff218823caf8825d5d72fd00`. `docs/servsync-master-plan/ServSync_Feature_Backlog.md` now tracks unfinished outcomes only, while `docs/servsync-master-plan/ServSync_Completed_Features.md` archives completed v1 milestones and `docs/servsync-master-plan/CHANGELOG.md` remains the detailed implementation and rollout history. Backlog and archive wording must continue to separate live beta capabilities from source-only, Sandbox-only, gated, cohort-bound, or future capabilities.

FB-003A Local-Customer Multi-Property Claim is complete. PR #348 merged the explicit selected-property invitation model and guarded token-free/create-or-match/atomic-acceptance workflow. Controlled Production confirmation covered application of the reviewed SQL plus catalog/security validation and public smoke; the full authenticated Tests A-H passed only in the dedicated Demo environment against source `6e5226e869e64bfed3102acfa09779b846cbbb88`. That Demo evidence covers multi-property creation and preview, atomic acceptance, one-time replay rejection, stale invalidation, legacy single-property compatibility, connected-homeowner null safety, tenant isolation, and token containment. No further authenticated FB-003A validation run is required. Raw expected-replay RPC/JSON detail remains separate P3 presentation polish under FB-036, and automated/provider delivery remains separate future work.

Provider-neutral integration readiness is now part of the data-safety roadmap. The integration foundation adds internal-only mapping and outbox seams for future reviewed work such as QuickBooks/accounting export, Stripe/payment events, calendar sync, notifications, document/e-signature, CRM/import-export, and mobile/offline coordination, but no live integrations are active from that foundation. Future provider-specific IDs should map through reviewed provider-neutral records or follow-on mapping tables rather than being added directly to core customer, estimate, job, invoice, home, or payment records without a separate approval.

Controlled Operations evidence is documented as an FB-020 operational-readiness track in `docs/CONTROLLED_OPERATIONS_RUNBOOK.md`. The current foundation is complete through Slice 2D-A and remains local/provider-neutral: it can produce provider-neutral operation packets, sanitized command evidence, packet-bound browser evidence, sequential browser-attempt lineage, interrupted-attempt reconciliation, browser-aware freeze/manifest/seal verification, expected external seal-digest verification, a provider-neutral adapter contract, a local fake provider, and local external-anchor custody reference behavior. It does not provide live provider authority, credential resolution, production target selection, deployment or SQL authority, automatic rollback, or a real Vercel/Supabase/GitHub adapter. Slice 2D-B live-provider adapter work remains deferred until a separately approved provider, credential, target, authorization, and rollback model exists.

Project Collaboration is now governed by a canonical product specification and begins with a hidden foundation only. The first foundation slice is intended to add durable project identity, property association, commercial project parties, individual memberships, Project Lead authority assignments, structured project events, and one-project-per-job association behind a disabled-by-default runtime gate plus private allowlist. This does not make Project Collaboration live, Beta-exposed, or production-ready; it does not add Project Board UI, assignments, invitations, project billing, financial sharing, estimate/invoice permission changes, production SQL application, production allowlist activation, or ordinary-user access.

Demo Mode / Marketing Capture Environment is now a support-infrastructure track for repeatable screenshots, recordings, QA demos, onboarding videos, and sales demonstrations. The first foundation slice is dedicated-demo-environment only: it adds private demo registry SQL, a private seed/reset/verify runner, guarded package scripts, a fictional water-heater core scenario through accepted-estimate job creation, and an internal runbook. Slice 2A extends that private runner with deterministic checkpoints through job creation: `contractor_discovery_ready`, `connected_request_ready`, `request_ready`, `contractor_review_ready`, `estimate_draft`, `estimate_sent`, `estimate_accepted`, and `job_created`, with checkpoint-aware verification and reset/rebuild behavior. The discovery checkpoint provides a pre-connection opening state for recording the existing homeowner contractor search/profile connection request path without creating Discover posts or browser checkpoint controls, while the connected-request checkpoint provides an active-connection/no-request state for recording homeowner service-request setup. Slice 2B extends the same private runner through lightweight job lifecycle checkpoints: `job_scheduled`, `job_in_progress`, `job_review_ready`, and `job_completed`, using the real visit-scheduling RPC where available and controlled fixture transitions where ServSync does not yet expose dedicated job-start/review RPCs. Slice 2C-A adds frontend-only presentation controls that can activate only in the dedicated demo Vercel/Supabase environment, hiding capture clutter and mutating controls while preserving read-only role-specific navigation and underlying product states. This does not create public Demo Mode access, role switching, browser checkpoint controls, reset/seed controls, media seeding, invoice/Home History/reminder demo data, finalized reports, production SQL application, shared sandbox use, deployment, external notifications, payments, or production data actions.

The automatic homeowner and contractor role-based onboarding tour has been removed. Onboarding should rely on actionable setup prompts and checklists in the product, with future help moving toward searchable, task-specific guidance and optional walkthroughs rather than automatic tour cards after setup.

# 2. Product Vision

The authoritative product vision, philosophy, mission statement, north star, target users, and long-term boundaries now live in [ServSync Product Vision & Philosophy v1](ServSync_Product_Vision_and_Philosophy_v1.md).

The authoritative platform information architecture now lives in [ServSync Product Information Architecture v1](ServSync_Product_Information_Architecture_v1.md). It defines the long-term homeowner and contractor portal mental models, module ownership boundaries, sidebar/navigation principles, subscription/access placement, and Draft-first Work placement.

The target contractor Work experience now lives in [ServSync Contractor Work Module Product Specification v1](ServSync_Contractor_Work_Module_Product_Specification_v1.md). It defines the Work mission, Work Dashboard, Draft-first lifecycle, Job Overview, Job History, Estimate and Invoice placement, Reports and Templates posture, scheduling relationships, contextual Start New Draft rules, permissions/entitlements, backend contract gaps, legacy migration boundaries, and future implementation-slice framing.

This Master Plan keeps the execution-facing summary: ServSync is a shared home-service platform that helps homeowners find and work with trusted contractors while helping contractors manage relationships, requests, estimates, jobs, invoices, reports, and connected property records without unnecessary enterprise complexity.

# Product Principles

## Draft-First Work

ServSync should treat a Draft as the universal starting point for contractor work. A Draft is the contractor's neutral workspace for capturing customer, property, source context, scope, labor, materials, notes, scheduling intent, and supported photos or attachments before the contractor chooses an intended output.

The approved Draft Launch Principle is: `A Draft is temporary planning. A workflow begins when the Draft is launched.` A Draft is private contractor planning, not yet an active operational workflow and not part of the homeowner's permanent record. The intended output may change before launch. Launching creates exactly one initial first-class workflow record through an authorized Estimate, Job, Inspection Checklist, or Invoice path. Once launched, the Draft is consumed and no longer remains an active Draft. Later records are created from the active workflow, not from the consumed Draft.

`Save Draft` is the pause-and-return option, not a required step before choosing an outcome. A contractor should be able to enter work once and proceed directly from the current composer state to `Create Estimate` or `Create Job` when those launch actions are implemented. Each launch action should persist the current Draft state before creating the selected workflow record, without requiring the user to save, exit, and reopen the Draft first.

The shared Draft Composer launch work should use a new default-off `VITE_SHARED_DRAFT_COMPOSER_LAUNCH_ENABLED === 'true'` gate during development. Small implementation slices may merge hidden into `main`, but the first coherent owner-facing Preview validation should include the shared composer, Estimate intent, Job intent, Create Estimate, Create Job, Draft consumption, duplicate prevention, resume behavior, and workflow navigation. Do not ask users to evaluate a half-complete shared Draft workflow, a selectable Estimate intent that cannot launch, or a Job-only shared composer that implies Estimate support is complete.

Slice 2A adds only the hidden frontend shared Draft Composer foundation: intended-output session state, supported shared fields, existing Draft Job save/resume persistence reuse, Save Draft, and Back to Work. It does not persist intended output durably, launch a Draft, create Estimates, create Jobs from the shared composer, expose Invoice as an outcome, change Production, or enable the gate.

Slice 2B implements the durable backend Draft Launch Foundation in merged PR #317 (`895d14c4f50435b079b02c34e63aa02238a2c755`). It introduces the dedicated `contractor_work_drafts`, `contractor_work_draft_items`, and private success-only `contractor_work_draft_launches` model, durable intended output, private notes and subject snapshots, consumed state, deletion-compatible private linkage and immutable output snapshots, one-Draft/one-launch constraints, idempotency, tenant authorization, relationship locking/revalidation, legacy import, Job property-sharing parity, operational service-request Job deduplication, retention, contractor-only RLS/grants, save/get/import RPCs, and atomic Estimate/Job launch. ServSync Sandbox has both the foundation and its permission-parity correction installed. The full runtime matrix passed one-Draft/one-launch, adversarial rollback, concurrency, relationship revocation and property-unsharing locking, deletion/retry, privacy, tenant isolation, RLS, grants, PostgREST, retention, and owner/admin/office/field-technician/viewer/platform-admin permission parity; all disposable fixtures were removed and no known Slice 2B runtime blocker remains. Production has the corrected canonical foundation installed and verified from hash `ac9e600ece3075e2d171da5571aab2b26e7a1f6f234239b02194fa7be3d2354f`. Draft persistence uses the union of existing billing-management and Job-write helpers, while each launch path retains output-specific authority. These helpers remain compatibility mechanisms, not permanent role policy, and no universal field-technician Estimate restriction was introduced. Sandbox received the earlier foundation before the capability-union fix and subsequently received correction hash `b4a98f33acd99083bea4497268393f6277ef330cdcb31bdc5d253adb94b14c7f`; neither SQL file should be reapplied there, and Production must not receive the Sandbox-only correction after the corrected canonical foundation.

The approved Slice 2C architecture audit split frontend adoption into bounded stages. PRs #318-#321 are merged: Slice 2C-A added typed durable adapters; Slice 2C-B added canonical save/resume/list/import; Slice 2C-C added launch commitment, same-key recovery, canonical consumed adoption, explicit Open Output, and guarded same-session confirmed Create and explicit Retry navigation with exact-output/token-owned focus. Slice 2C-D completed authenticated Sandbox/Preview runtime and source-level mobile validation. External, mount, list, lifecycle-only, and background recovery remain explicit-only; navigation failure preserves the consumed Draft without relaunch or a new key. Later merged work added separately authorized Inspection Checklist and Invoice launch paths while preserving output-specific authority.

The Draft-first Inspection Checklist Job path is merged and its reviewed SQL foundation is installed in Production. It adds a separate `inspection_checklist` work format for private durable Drafts, lets a contractor snapshot a starter, contractor-owned, or eligible Home-specific Inspection Checklist source, and routes checklist Draft save/launch through dedicated checklist-specific RPCs. Home-specific checklist eligibility requires the exact selected property, starter snapshots must match the canonical source catalog, launched checklist prompts start as `Not Recorded` rather than pre-answered or substantive findings, and duplicate checklist room/item structures fail validation. No operational report or Job is created before launch, and checklist Drafts launch only through their separately authorized Job path. The standard Production Draft-first exposure does not replace those output-specific checks; authenticated Production checklist save/launch evidence remains a controlled follow-up.

Trade Packs extend this same Draft-first and Jobs lifecycle through contextual, ordered specialized sections rather than separate trade applications or navigation. Trade Pack Domain Contracts v1 establishes hidden default-deny workflow-family, trade, capability, work-type, immutable version, visibility, and downgrade contracts. Capability identifiers remain independent of Stripe, product, price, tier, trial, discount, and final packaging decisions. The disabled skeletal HVAC No Cooling definition is contract-validation data only; it contains no unreviewed diagnostic content and grants no contractor access. Property Asset Bridge v1 extends canonical `home_assets` with generic customer-safe identity, local-property claim continuity, append-only revisions/provenance, exact-revision concurrency, and controlled property-derived authorization. Durable Trade Section Instances v1 adds the hidden runtime identity joining an exact published definition and independent snapshot to the canonical Draft/Estimate/Job lineage, exact connected or contractor-managed property, optional accepted asset revision, strict bounded values, append-only history, optimistic concurrency, and active/completion-only/revoked capability semantics. Draft launch, accepted Estimate-to-Job conversion, and local-property claim mapping preserve the same section UUID. Owner/Admin/Office may mutate; Viewer is exact-work read-only; Field Technician and homeowner raw-section access remain deferred. On 2026-08-09 the exact three-migration chain was validated in Demo and then applied independently to Production, so Sandbox, Demo, and Production now share these hidden backend foundations. Demo's curated lowercase `plumbing` asset retained its exact identity and fingerprint through the RPC path; Production retained zero assets and revisions. All environments remain inert with zero capability grants, enabled work types, Trade Section instances, and Trade Section revisions. The Assets & Systems client now resolves the bridge RPC directly in every environment; its exact missing-routine-only legacy fallback remains a temporary compatibility guard rather than an active rollout dependency. Recognized historical category casing is compared with the bridge kind through the migration's strict normalized mapping, while the exact stored category value remains unchanged; unknown and conflicting identities fail closed. Runtime Slice 1A adds a strict default-off Job-detail client boundary for schema-v1 scalar readings/tests, server discovery, exact-Job loading, idempotent creation, and optimistic updates. Runtime Slice 1B temporarily enabled one fictional scalar-only Sandbox definition and exact contractor grant through an isolated branch Preview, proving Owner/Admin/Office mutation, Viewer exact-Job read-only behavior, excluded-role/tenant/Job denial, idempotency, contiguous revisions, stale-write handling, interrupted-response non-retry, mounted revocation, and responsive rendering. Every temporary database and Preview artifact was removed by `2026-08-09T16:33:34.918Z`; Sandbox returned to the exact inert baseline, and Demo/Production were unchanged. This is validated hidden runtime infrastructure, not product release: the gate remains unset everywhere, and no visible Asset/HVAC change, professional content, customer projection, recommendation conversion, report/history output, work-type enablement, or capability grant exists.

Durable Draft contractor rollout uses a tenant-specific default-deny exposure decision without changing backend Draft authority. PR #322 added `contractor_billing_accounts.durable_draft_beta_enabled boolean NOT NULL DEFAULT false` plus a narrow owner/active-team-member RPC. The frontend shows durable Work only when all three strict global gates are true, Demo Presentation is off, a current contractor/session entitlement resolves true for the exact canonical UUID tenant, and the existing independent persistence or output authority permits each action. Loading, missing, invalid, foreign, stale, and failed entitlement states show legacy Jobs. Platform-admin status alone is not contractor context. Equivalent focus/visibility refreshes for the same client, tenant, session, and generation share one in-flight request; context changes and stale results remain generation-guarded. PR #352 added a private runtime setting that defaults to `cohort` and can resolve valid contractor contexts through `all_contractors` without setting billing rows true, bulk-enrolling accounts, or replacing role/action helpers.

On 2026-08-02, the owner-approved Production rollout verified the complete installed durable Draft catalog and security foundation, confirmed the active READY Vercel deployment `dpl_4XotZUAJFBqmEqqVtiz1EyLNyyQi` at main SHA `b23e18708601c515ff218823caf8825d5d72fd00`, confirmed all three global gates compiled as `true`, and changed only the private server rollout setting from `cohort` to `all_contractors`. Existing contractor billing rows and application-record counts were unchanged. Public smoke passed 7/7 before and after activation; dedicated homeowner read-only smoke passed, and contractor owner read-only validation confirmed the Draft-first Jobs landing at desktop 1280x720 and mobile 390x844 with zero console errors or failed requests. Mutating Production Draft save/reopen/launch smoke and optional field-tech/viewer account coverage remain separately controlled follow-ups. A continuously active uninterrupted tab still has no autonomous entitlement-refresh maximum because the design intentionally adds neither polling nor server push.

Contextual product surfaces may offer `Start New Draft` when the screen has enough trusted context to help the contractor start faster. Good candidates include homeowner/customer profiles, property/home profiles, service requests, calendar appointments, prior jobs or work-history records, and lead/opportunity screens. The Draft should prefill only context already known and permitted from that surface, such as customer or property identity, request description, trade, photos, appointment date/time, visit notes, or selected prior scope for `Start Similar Draft`.

Do not scatter Draft actions indiscriminately. Settings, generic reports, pricing administration, and unrelated configuration screens generally should not expose `Start New Draft`.

Starting or saving a Draft must not automatically activate a job, create an estimate, create an invoice, request approval, create an appointment, send a notification, create customer-facing activity, create billing activity, finalize a report, or expose the Draft to a homeowner. A contextual Draft should preserve source context, reuse existing relationships, avoid duplicate customers/properties/requests/jobs/work records, support traceability and future analytics, and avoid asking the contractor to re-enter known information. Duplicate launch prevention must keep one active Draft from independently creating multiple unrelated permanent outputs forever.

User-facing naming should prefer `Start New Draft`, `Start Similar Draft`, `Drafts`, and `Continue Draft`. Avoid using `Draft Job` as the long-term general user-facing name when the record has not yet become a job, and avoid separate universal creation entry points such as `New Estimate`, `New Job`, or `New Invoice` when the intended workflow should begin from the shared Draft. Specialized document creation may still exist where explicitly justified, but new workflows should first be evaluated against the Draft-first model.

The intended mental model is:

- Draft: neutral editable starting workspace.
- Job: live operational record after activation.
- Estimate: customer-facing proposal snapshot.
- Invoice: customer-facing billing snapshot.

The current technical implementation may evolve incrementally, but future design should move toward this consistent mental model.

## Marketing Workspaces

ServSync Marketing should develop as one shared Marketing Engine/domain with audience-specific workspaces, not as unrelated internal and contractor products. The first audience is the private ServSync internal workspace for operating ServSync's own marketing. A later, separately authorized Business Marketing workspace may reuse the same bounded domain contracts for contractor tenants while deriving its authorization and data from the exact contractor context. Homeowners are not a Marketing workspace audience.

Marketing Foundation Slice 1 is source-only and private to the existing `platform_admin` application shell. It establishes reusable audience, navigation, metric-state, approval, schedule, and recommendation contracts plus an honest Overview and foundation destinations for Content, Campaigns, Prospects, Growth, and Settings. Current contractor/homeowner account totals and active invite counts may appear only from existing exact platform data; publishing activity is zero for this workspace, while website visits and acquisition attribution remain explicitly not connected or unavailable. No marketing persistence, social or email provider, scheduled worker, prospecting, outreach, AI-generated content, analytics integration, contractor-facing UI, homeowner-facing UI, billing, or Production configuration is introduced.

Marketing Foundation Slice 2 adds the first durable internal content queue behind the same private workspace. One provider-neutral workspace identity, content record, optimistic revision, and append-only status-event model supports `idea -> draft -> needs_approval -> approved`, with explicit return-to-draft and rejection decisions from `needs_approval`. The internal Content destination now supports create, eligible draft edit, submit, filter, detail, approve, return, and reject; the Overview reads the real approval queue. All authority and workspace identity are derived server-side through platform-admin-only RPCs over forced-RLS, policy-free private tables. The exact foundation is applied in Sandbox, Demo, and Production with one empty internal workspace per environment and zero runtime content/history fixtures, while publishing, AI generation, external integrations, campaigns, analytics, outreach, and contractor/homeowner Marketing remain unavailable.

Marketing Slice 3 pivots to Codex-assisted draft preparation instead of an in-app AI provider. A versioned Marketing Truth Pack, coordinated provider-neutral recipes, an exact package schema, and a platform-admin-only atomic ingestion boundary prepare durable internal drafts with immutable provenance and replay protection. Every accepted item begins as `draft` and continues through the existing human approval workflow; Codex receives no submit, approve, schedule, or publish authority. The exact foundation is applied in Sandbox, Demo, and Production with one internal workspace and zero preparation packages, content rows, or status events per environment after rollout validation. Runtime AI generation, canned fallback copy, provider coupling, publishing, and external Marketing audiences remain unavailable.

Marketing Slice 3A adds a provider-neutral Marketing Direction between product truth and recipe execution. New packages preserve one approved direction statement through the existing immutable package brief, while the source contract distinguishes owner-led and recommended directions, captures bounded assumption corrections, and rejects unsupported competitor contrasts before copy preparation. Truth Pack v2 documents the real choice between secure document-specific interaction without a registered account and the optional connected-homeowner relationship, and recipe v2 gives each package role a distinct purpose under one central message. Historical v1 contracts and the first Production package remain unchanged. The exact additive compatibility migration is applied and validated in Sandbox, Demo, and Production so the existing RPC accepts v1 replay plus v2 packages and applies the new guard only to v2 direction/copy. No new table, role, authorization surface, runtime generation UI, provider, approval, publishing, or scheduling behavior is introduced.

The intended eventual operating loop is: system prepares marketing work, the user reviews meaningful decisions, approved work is scheduled or published, and real results support recommendations. Each capability must be introduced through a separately reviewed slice that preserves truthful data states, internal-versus-tenant authorization, provider boundaries, auditability, and fail-closed behavior. The initial workspace must not imply that those automations already exist.

# 3. Plain-Language Product Definition

ServSync connects homeowners with local service contractors and keeps requests, estimates, jobs, invoices, communication, and home service history organized in one place.

Simpler platform wording should be preferred when describing ServSync publicly. Avoid overcomplicated language like “integrated homeowner-contractor workflow ecosystem” unless writing internal strategy. Public copy should be direct and credible.

| Audience | Plain-language message |
| --- | --- |
| Homeowner | Find local contractors, request work clearly, and keep your home service history organized. |
| Contractor | Get discovered by local homeowners and manage the work professionally from first request to final invoice. |
| Platform | Find local contractors. Keep the work organized. |
| Investor/partner | A marketplace and workflow platform connecting local homeowners and small contractors through permission-based relationships and service records. |

Contractor team role boundaries should stay simple for v1: owners, admins, and office users may manage billing/money actions, while field technicians are limited to job execution work such as notes, findings, photos, checklist/work completion, and similar operational updates. Field technicians should not create, send, void, or mark invoices paid, change customer-facing financial terms, or create/edit price-bearing work items unless a later explicit permissions model is approved.

# 4. Target Audience

ServSync should serve all small contractors broadly, but the first design and pricing decisions should favor solo operators and very small teams. The user is likely someone who gets work through referrals, Facebook, word of mouth, repeat customers, and local visibility, but lacks a clean system for the work after the lead appears.

| Segment | Priority | Notes |
| --- | --- | --- |
| Solo contractors | Highest | Best early target; simpler onboarding, lower price sensitivity threshold, clear need for professionalism. |
| 2-person contractors | High | Still close to the solo workflow; pricing should not punish first growth step. |
| 3-5 person teams | Medium later | May need team roles, scheduling, dispatch, and stronger admin controls. |
| 10+ tech companies | Low for early stage | Likely already compare against larger FSM products; avoid building for them too early. |
| Homeowners | Strategically high | Free users create marketplace demand and long-term home records. |

# 5. Strategic Positioning

ServSync should not try to look like a smaller ServiceTitan or a generic Angi alternative. The positioning should sit between contractor software and homeowner discovery. Most contractor software assumes the contractor already has the customer. Most lead sites help the homeowner find a name and then disappear. ServSync should connect discovery to the actual work lifecycle.

- Not just a lead site: ServSync supports what happens after a homeowner finds a contractor.
- Not just contractor software: ServSync gives contractors homeowner-facing exposure and connection opportunities.
- Not just records storage: service history becomes useful because it is connected to requests, jobs, contractors, invoices, and reminders.
- Not enterprise software: early product should remain simple, affordable, and practical for small contractors.

# 6. Core MVP Loop

The product should be judged against the core loop: Homeowner Request -> Contractor Estimate -> Homeowner Approval -> Job -> Completion -> Invoice -> Home History -> Reminder. Recent v1 cleanup work has made this loop more visible and easier to follow for both homeowners and contractors. The remaining work is deeper workflow hardening, automation, end-to-end coverage, and beta readiness rather than basic loop discovery.

| Step | Current understanding | Priority |
| --- | --- | --- |
| Homeowner Request | Homeowners can create requests tied to homes/properties. Direct connected-contractor flow exists. Recent UI copy clarifies what happens after a request is sent and after an estimate is accepted. FB-021 Scheduling + Appointment Confirmation Foundation Phase 1 adds a SQL/RPC/RLS foundation for contractor-proposed appointment windows, homeowner acceptance, contractor reschedule/cancel, and appointment event history while keeping existing single appointment summaries for compatibility. Phase 2 wires read-only request-card display for confirmed appointment summaries and pending proposed visit windows on homeowner and contractor request surfaces. Phase 3 adds the first safe action UI: contractors can propose 1-3 visit windows on active requests, and homeowners can accept or decline proposed windows through RPC-backed controls. The FB-021 closeout package adds contractor replacement-window reschedule controls, contractor appointment cancellation controls, and compact appointment-state helper copy using existing RPCs; email/calendar sync, dispatch, routing, GPS, notification delivery, and homeowner-forced booking remain deferred. FB-025 Slice 1 adds a SQL/RLS/test foundation for future workflow-scoped service request and job messages, append-only activity events, and thread read tracking, without replacing existing request messaging or adding UI/email/SMS/push delivery. Slice 2A adds the first job-centered message thread UI only where the job is linked to a homeowner-visible non-draft estimate and request, preserving legacy request messaging and deferring unread badges, request-message migration, and notification delivery. Slice 3A adds a read-only derived Activity timeline for major request/job workflow updates on homeowner related updates and contractor job detail surfaces without writing durable activity events. Slice 3B adds durable `workflow_activity_events` writers for selected trusted lifecycle RPCs only, covering appointment proposed/confirmed, estimate approved/declined, job created, and invoice sent/paid/voided events. Slice 3C merges approved durable activity rows into the existing Activity UI under RLS using only known visible context IDs, while preserving derived fallback rows for older records. Slice 4A adds first in-app job-message indicators on eligible homeowner request/job-thread and contractor job surfaces using existing `workflow_messages`, `workflow_thread_reads`, and read-marker behavior only; SQL, Activity unread indicators, request-message migration, and notification delivery remain deferred. | Harden end-to-end coverage and beta QA; continue appointment-window action slices carefully without jumping to calendar sync, dispatch, or routing automation; keep communication work tied to homeowner-initiated or eligible workflow contexts rather than contractor cold outreach. |
| Contractor Estimate | Estimate creation, line items, templates, notes, terms, sending, PDF download, Price Required/unpriced line handling, saved charges, and a rule-based Build Estimate Draft workflow exist. Build Estimate Draft now runs inference only on Build, keeps trade tools behind an advanced section, supports current-draft labor format choice, and avoids duplicate generated builder content on rebuild. FB-019 Estimate Labor Model v1 is complete: PR 1 SQL/schema foundation and PR 2 SQL-only backend conversion preservation are merged and applied to sandbox/production, PR 3 adds app/UI support for contractor labor controls, save/reopen, totals, homeowner display, PDF display, and minimal invoice preservation, and PR 4 aligns Build Estimate Draft with the schema-backed labor model so line-specific mode no longer creates generated standalone labor rows. The live v1 model uses one estimate-level labor rate, job-total labor hours, line-specific labor hours on material/scope lines, and customer-facing material/labor/tax total separation; homeowner/PDF display shows filled labor hours and hides blank labor hours. Estimate Helper v1 adds a code-only contractor suggestion panel for commonly missed optional scope/revenue items; suggestions are never auto-added, added items become normal editable Price Required lines, and helper rationale must stay editor-only. App-created estimate/invoice line types should use labor, material, fee, and other; equipment-style items should be treated as material while SQL cleanup removes legacy equipment allowance at the database layer. Structured line item app support now treats `line_title` as the primary customer-facing Description, keeps `description` as a legacy fallback, preserves old `customer_description` data for compatibility, and keeps optional model/spec and supply status display cleanly hidden when blank. Starter estimate templates now include more specific structured trade starters for HVAC system replacement, plumbing water heater replacement, electrical dedicated circuit / EV charger work, carpentry deck repair/build work, and generic service calls. Estimate Draft Library path-flow v1 introduces a price-free code-only foundation for trade/work-category/job-bundle recommendations, beginning with one HVAC replacement proof bundle and preserving the existing rule-based fallback when no bundle matches. Price Book foundation v1 adds private contractor-owned records, a contractor Jobs-tab management surface, CSV-only import with preview/mapping, and an Estimate-only quick-pick that copies active items into normal editable lines while keeping internal notes out of customer-facing output. The management surface now leads with search, active/archived views, type/trade/category filters, compact paged rows, a short form with Advanced Options, explicit load/error/empty states, and secondary CSV tools; visible labels use Price Book while stable internal identifiers remain unchanged. Draft-first Price Book Picker v1 reuses the same safe snapshot mapper for standard Estimate-intent Drafts, adds one active item at a time as a fresh editable line, preserves blank Price Required versus explicit zero, and does not autosave, launch, mutate or link the source record, or copy private Price Book metadata. Price Book remains distinct from saved charges, estimate templates, Inspection Checklists, helper suggestions, Build Estimate Draft, invoices, XLSX/PDF imports, AI suggestions, homeowner views, and plan-tier enforcement. Recent contractor cleanup makes Create/Open Estimate and accepted-estimate next steps clearer. The normal estimate creation path now uses clearer blank/template/draft-estimator starts, keeps blank/template creation in one single-page quote form, treats templates and Draft Builder as start paths, exposes saved charges and Price Book as contextual line-item sources, starts blank estimates with an empty line-item choice, keeps add controls below existing line rows, hides broader create controls while a draft is active, and places advanced line metadata behind `More details` controls without changing estimate persistence or lifecycle behavior. The contractor draft estimate editor includes a structured Payment schedule for full-on-completion, deposit/final, or custom billing plans. Accepted Estimate Deposit Workflow v1 adds a compact provider-neutral billing summary and a deliberate `Request deposit` action for exactly one Deposit item. The server derives the amount from the accepted schedule snapshot, atomically creates or reopens one linked draft Deposit Invoice, and never sends it automatically. Deposit status is visible without blocking Create Job. | Keep estimate path clear from invoice path, keep structured payment schedules provider-neutral and honest about customer visibility, preserve Job creation independently of deposit status, and keep rule-based drafting, library bundle recommendations, helper suggestions, saved charges, CSV-imported Price Book records, and manual Price Book records distinct. Stripe and online payment enforcement remain separately gated. |
| Homeowner Approval | Accept/decline status and RPCs exist. Homeowner accepted-estimate copy now explains that the contractor's next step is to create, schedule, or complete the job/work. Contractor dashboard `need jobs` CTAs now route directly to the Approved estimates view with stale filters cleared, and accepted estimate cards distinguish Ready for job from Job created while keeping Create/View Job primary. | Validate the full approval-to-job handoff in beta. |
| Job | Jobs are backed by the inspections table; simple service jobs and checklist/report jobs exist. Contractor-facing terminology has been partially cleaned up, while internal schema names may still use inspection terminology. Draft-first is now the standard Production Jobs experience under the reviewed `all_contractors` rollout mode and all three compiled Work/Draft gates. Durable Draft save/resume, standard Estimate and Job intents, checklist snapshots, Job launch, Jobs landing navigation, Saved Work Templates, and the Estimate-intent Price Book picker remain bounded by existing output-specific authority. The blank-resume corrective slice refetches canonical Draft inspection and scope rows before editing and preserves supported connected/local customer selections and scope fields. Draft-launched Jobs preserve the Draft/job UUID and scope-item UUIDs. Checklist Drafts remain Job-only, require a bounded versioned checklist snapshot, and do not require work lines. Draft-first Inspection Path Completion v1 is merged and deployed to Production: contextual connected/local customer starts converge on the shared composer while retaining the legacy fallback when Draft-first is unavailable, home-specific templates remain bound to the exact selected property, `Not Applicable` is a neutral recorded finding, and reviewed report/media authority hardening is installed. Partial-invoicing foundations continue to use durable job work items, while checklist findings remain separate from priced work-item conversion. Jobs without durable source-backed rows keep existing task/checklist behavior without fake migration or global backfill. | Keep the bounded Production path and legacy/no-work-item Jobs compatible, and separately plan broader trade-specific checklist maturity, finding-to-follow-up-work conversion, explicit legacy conversion/backfill, AI/Edge Function prompt parity, later Invoice wiring, and reopening/correction rules. |
| Completion | Finalization/report filing exists. Recent contractor cleanup keeps invoice creation behind report finalization for checklist/report jobs. Partial-invoicing foundation work is designed to derive backlog/open labels from work item counts rather than adding broad new job status values. | Add broader regression coverage for closeout paths and future backlog-open/partial-complete UI behavior. |
| Invoice | First-class invoices exist. Homeowner invoices can be filed to Home History through a durable invoice relationship without duplicate PDF storage, and filed homeowner invoice cards now show a direct `View Home History` next action plus compact manual-reminder helper copy regardless of paid status. Partial-invoicing UI wiring can create draft invoices from selected completed, billable, priced durable job work items and shows invoice backlog snapshots separately in contractor invoice cards, homeowner invoice cards, and invoice PDFs. Backlog rows are context only and do not affect subtotal, tax, or total. The whole-job invoice path remains a fallback for legacy/no-work-item jobs, while the guardrail slice routes work-item-backed jobs toward item-based invoicing and adds a SQL patch to reject new whole-job invoice creation when durable work items exist. The contractor completed-job invoice UI now explains that distinction with recommended-path copy without changing eligibility or creation behavior. FB-020 immutable invoice hardening now treats direct invoice and invoice-line edits as draft-only, leaving customer-facing status changes to explicit RPC transitions such as send, homeowner view, record payment, and void. A multiple-invoices foundation adds structured invoice purpose/order fields (`invoice_type`, nullable `invoice_sequence`) for total/deposit/progress/final workflows. Accepted Estimate Deposit Workflow v1 creates one amount-snapshotted draft Deposit Invoice from an accepted schedule item, preserves unpaid void history while permitting one replacement, and adds a private append-only offline-payment ledger. Authorized financial roles can record cash, check, bank transfer, external terminal, or other payments with date, actor, optional reference/note, idempotency, partial balance, and paid-state transitions. Estimate billing separates invoiced, paid, remaining scheduled, and remaining Estimate amounts so deposit money is not added above the agreement total. Connected and Not connected Customers continue through their existing authenticated or request-free Invoice delivery paths. The exact backend migration is applied and catalog/preservation-validated in Sandbox, Production, and Demo without activating Stripe or changing legacy balances. | Keep Invoice actions focused and preserve one billing obligation across connection states. Defer Stripe/Pay Now, provider processing, refunds, chargebacks, automatic payouts, historical payment-ledger backfill, broader accounting export, and arbitrary invoice creation until separately approved. |
| Home History | Home History is the homeowner-facing destination for completed work, filed reports, invoices/receipts, notes, warranty context, and service records. It uses existing maintenance-log/database structures internally. Filed invoice cards now route homeowners back to the existing Home History view so they can find the filed record and manually add a follow-up reminder there. | Avoid overbuilding a new timeline, automatic reminders, notification delivery, or recurrence until beta feedback proves the need. |
| Reminder | Homeowner-only manual Home Reminders v1 is shipped. It supports in-app reminders tied to homes/Home History context without email, text, push, recurrence, scheduler, or calendar sync automation. | Keep automation future-facing until manual reminders are validated. |

PR #367 completed the bounded Draft-first Inspection Path Completion v1 Production rollout through merge commit `052d13ca061653f0bfa6bad4353d2f47eb95c205`, the catalog-verified inspection-path migration, and automatic `READY` deployment `dpl_EVyf2uB49Uq53ufzw6ZwG9n7z8Vd`. Production validation covered public health, contractor-owner Jobs access, homeowner Documents access, desktop and mobile layouts, anonymous protected-RPC denial, homeowner contractor-management denial, clean console/network health, and preservation of business counts plus database/storage security posture. It did not exercise finalized-report download, cross-homeowner finalized-report access, admin/office/field-tech/viewer/second-contractor roles, or mutating checklist, media, and finalization paths; exact-head Sandbox evidence remains the basis for those paths. This completed slice does not close FB-007's broader inspection/checklist maturity work.

Contractor Work Dashboard Slice 1 introduced the frontend Work landing shell behind the strict `VITE_CONTRACTOR_WORK_UI_ENABLED=true` flag. The owner-approved Production rollout now enables this shell and Draft-first globally while retaining the `Jobs` page identity, existing output-specific authority, and server-side fail-closed checks. No new global route or mobile navigation item was introduced.

The first Jobs landing redesign keeps the internal Work architecture but uses `Jobs` as the visible page identity. Its gated overview has one compact At a Glance navigation system for Needs Attention, Drafts, Estimates, Active Jobs, and Invoices; full records move to dedicated destinations. Needs Attention is intentionally limited to accepted estimates without a Job, completed invoiceable Jobs without an Invoice, and draft/overdue/partially-paid Invoices. Actions & Tools reuses the existing Start New Draft, Templates, Service Plans, and Price Book destinations and their existing role/capability checks. The slice is frontend-only, preserves the legacy fallback, and does not alter SQL, RLS, RPCs, entitlements, configuration, Production gates, or provider delivery.

Draft-first Saved Work Template integration keeps the Inspection Checklist path separate while letting the standard Draft Composer apply existing contractor-owned saved estimate templates as reusable scope, private notes, and supported line-item content. The integration reuses already loaded, contractor-scoped template data, does not add SQL/RPC/RLS changes, does not create Estimates, Jobs, Invoices, or provider deliveries when a template is chosen, and does not change Draft-first rollout gates.

Draft-first Price Book Picker v1 keeps Price Book selection limited to standard Drafts whose intended output is Estimate. It reuses the legacy estimate quick-pick's shared safe snapshot mapping, searches and filters already loaded active/non-archived contractor items, and appends one fresh editable Draft line with quantity `1`. Blank Price Required and explicit zero remain distinct. Internal notes, SKU, source, trade, category, taxable metadata, Price Book IDs, and live linkage are not copied; selection does not autosave, launch, replace existing content, or mutate the source record. Saved Charges, templates, Inspection Checklists, Job/Invoice Draft selection, invoice quick-pick, multi-select, quantity staging, assemblies, default quantity/tax/category propagation, backend pagination, and authority redesign remain separate work.

Price Book Organization Foundation v1 extends the provider-neutral management model to `trade -> category -> subcategory`, adds current-page-only bulk organization and archive/restore actions, and lets generic CSV imports map optional subcategory values. Existing records remain valid with a blank subcategory, and Draft-first plus legacy estimate selection continue using the bounded customer-safe snapshot mapper without organization metadata or live linkage. PR #365 is merged and its additive `contractor_price_book_items.subcategory` migration was applied through the controlled Sandbox and Production rollout.

Price Book Repeat-Import Reconciliation v1 replaces the browser's direct add-only CSV insert path with tenant-owned stable import sources, deterministic server-authoritative matching, explicit Add/Update/Skip review, three-way preservation of conflicting manual edits, one idempotent all-or-nothing execute RPC, private external-object mappings, and immutable sanitized batch-row audit. Review rows are classified as New, Unchanged, Changed, Ambiguous, or Invalid; exact repeated rows without stable IDs and multiple rows resolving to one existing item fail closed before mutation. Raw CSV files are not retained, filenames are not identity, other rows without stable external IDs remain deliberate Add/Skip duplicate-warning flows, and blank Price Required remains distinct from explicit zero. PR #366 is merged and migration SHA-256 `7dac1b3c7fd62240d9d05e498a3db0af39fe93549c375ca8262f941cbb6d14a3` was applied through controlled Sandbox and Production rollout. It does not add rollback mutation, XLSX, cost/margin, Saved Charges, assemblies, export, provider-specific parsers/APIs, scheduled sync, or provider delivery. FB-024 remains active for those deliberately separate maturity slices, beginning with guarded import-batch rollback.

Request-Free Local Invoice Delivery Slice 1 is operational in Production from merged PR #369 at commit `bb471e2d62cd70abcdd66cb57582e660e97af1cd`. Authorized contractor owners, admins, and office users can issue an eligible saved invoice for one unclaimed contractor-created local customer/property and create, rotate, or revoke one expiring document-specific Copy Link. The flow generates at least 256 bits of entropy, stores only SHA-256 token/session digests, returns the raw link once, and uses a same-origin Vercel Function with server-only `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`; no privileged `VITE_` configuration is used. Browser bootstrap removes the visible bearer before React loads and exchanges it for a fixed-lifetime 30-minute `__Host-servsync-invoice-session` cookie with `Secure`, `HttpOnly`, `SameSite=Strict`, `Path=/`, and no `Domain`. Recipient requests are serialized to prevent stale cookie commits, ineligible or malformed access fails generically, direct browser/PostgREST execution is closed, and the Production Firewall applies an IP-keyed fixed-window limit of 100 requests per 60 seconds. Rotation, revocation, expiration, and voiding fail closed; claiming invalidates an established session, while an otherwise active pre-claim bearer may be reopened until expiration or revocation and create/rotate remain denied after claim. Link activity is not represented as delivery confirmation, receipt, acceptance, signature, or payment. All three reviewed migrations were applied transactionally to Production in order, deployment `dpl_7JKiCdPfCN8XKdMBVUyxKFv5AvKU` reached `READY`, and controlled Production validation passed the 9/9 smoke suite plus the bounded issuance, denial, desktop/mobile, token/cookie, concurrency, invalidation, request/response-limit, throttling, and cleanup checks. FB-003B remains active for anonymous PDFs, immutable/versioned estimate delivery, finalized reports, broader claimed-account transition, provider channels, reminders, and approval/payment/reply actions.

Not-Connected Customer Capability Parity v1 extends that proven document-scoped model to eligible saved Estimates without making Connected and Not connected Customers technically identical. Owner, active Admin, and active Office can publish an immutable customer-facing Estimate snapshot, then copy, rotate, or revoke one expiring recipient-specific link. The public route exchanges a one-time 256-bit bearer for a digest-only fixed 30-minute protected session through the same-origin server gateway; private forced-RLS tables, service-only recipient RPCs, authenticated tenant-derived management RPCs, generic denials, bounded response/rate limits, and exact snapshot allowlisting keep customer contact fields, contractor-private customer/property notes, tokens, broader history, approval, signature, payment, and mutation authority out of the guest boundary. Connected Customer in-app send, review, approval, Home History, notification, claim, sharing, and property behavior remains unchanged. The exact migration (SHA-256 `016a532e5b0db0ea48966a04091a741806406e1d32601b11885474547c2b29ad`) is installed and catalog-verified in Sandbox, Production, and Demo; Demo operational access remains subject to its existing fail-closed server gateway configuration. Report delivery, provider email/SMS, anonymous PDF download, acknowledgment/acceptance, signatures, payments, replies, a guest portal, Service Plan parity, and complete Connected/Not connected capability parity remain separate work.

Draft-Optional Contractor-Customer Migration Compatibility defines one bounded customer backend for environments with or without Durable Draft. Versioned read/list and archive migrations preserve the immutable Production/Sandbox migration checksums. An absent Draft foundation installs non-Draft customer directory, manager detail, role-shaped reads, creation, direct-table cleanup, claim/invitation protection, non-Draft historical context, and archive/restore with stable zero/empty Draft response values and no placeholder Draft objects. The complete mode is fingerprinted from the real canonical migration chain, including structural, policy, trigger, ownership/RLS, exact ACL, RPC, search-path, and non-delegable grant contracts; partial, reduced, drifted, or insecure foundations fail transactionally before compatibility or lifecycle DDL.

Demo `bdytwgejqnlblhrnqxkp` now carries the current Production-supported backend schema after the ordered repository migration chain and Demo-target-guarded reconciliation migration `servsync-demo-production-schema-parity-reconciliation.sql` (SHA-256 `6e2641f94c96075b20b3a27ff483c84541a06c83c3ddd4edb805c8675d9d701f`). Production-owned relations, columns, constraints, indexes, triggers, policies, functions, types, extensions, and default ACLs match exactly; Demo's only catalog additions are its private scenario tables and trusted operator functions. Authenticated role, customer directory/management/creation, claims/invitations, direct ACL, archive/restore, preservation, and desktop/mobile checks passed. Demo Presentation remains enabled. Canonical Durable Draft backend support is installed but its rollout setting remains disabled and the three Draft UI gates remain false/absent, so the Draft-first UI stays hidden. Request-free invoice delivery database support is installed but remains fail-closed until separately authorized Demo server credentials and Firewall configuration exist. Project Collaboration remains absent because it is absent in Production. No Vercel configuration, feature flag, entitlement, Production, or Sandbox state changed; backend schema compatibility does not claim full visible or operational Demo parity.

Backend Environment Parity Guard v1 establishes Production as the immutable supported-schema authority and Demo as the normal supported peer. The explicit read-only operator command compares logical `public` application schema, bounded `storage.objects` RLS state, ServSync-owned storage policies, functions/overloads, ownership/security/search paths, grants, column/table ACLs, and relevant default ACLs while ignoring business data, auth users, secrets, configuration, Presentation mode, feature gates, entitlements, and other operational differences. Exact selectors plus category counts, object-key fingerprints, and logical catalog fingerprints pin only the Demo scenario and Sandbox Project Collaboration additions; intentional rules apply only to additions and never mask changed Production-supported objects or unexpected security children. Function comparison removes only proven-safe comment/layout differences and keeps unproven lexical equivalence as drift. Current Production/Demo parity passes with the exact reviewed Demo scenario group. Sandbox retains the approved 297-object Project Collaboration experiment but still has the same 22 unexplained supported-schema findings, so it remains pending rather than silently aligned. Stripe Connect Online Payments Foundation is now supported schema across Sandbox, Production, and Demo and no longer requires a Sandbox exception. Sandbox Legacy Storage Policy Containment v1 removed the two unapproved broad authenticated `photos`/`reports` policies without changing bucket visibility, stored objects, other policies, ACLs, or Project Collaboration; the two legacy photo objects remain in their public bucket pending a separate retention/migration decision. The rollout ledger is descriptive and cannot override live failure. Live parity remains an explicit controlled command, not a standing credentialed CI gate.

Public Signup Role Hardening v1 treats auth signup metadata as untrusted and makes `servsync-public-signup-role-hardening.sql` the canonical last signup/referral trigger definition for clean installs and existing environments. Only `homeowner` and `contractor` are valid self-service profile roles; unknown, malformed, internal, platform-admin, and contractor-team role values resolve to the existing safe homeowner default. Authenticated clients may create a missing homeowner/contractor profile and update ordinary profile fields, but cannot assign a privileged role or change an existing role. Existing legitimate platform administrators are not rewritten. Universal referral codes plus one-time and permanent contractor referral attribution remain intact. PR #387 merged normally as `20e1b04adafaa5f5919a43b6d9fc9f4ede4cc74d`, and the final fixed-path-safe migration (SHA-256 `4fe4f48a57cfeb3a46a07bcfe67f2e716ea9be29e369324da225b244d4be6f7a`) is installed and validated in Sandbox, Production, and Demo. Exact function/trigger/security correspondence and captured-data preservation passed in all three environments; automatic Production and Demo deployments reached `READY` from the merge SHA without configuration, feature-flag, entitlement, membership, or unrelated business-data changes.

Secure Estimate Email Delivery v1 extends the existing request-free Estimate snapshot rather than introducing another guest model. Owner, active Admin, and active Office users can confirm a delivery-specific email address, send or resend a branded message, and review sanitized attempt history; editing the delivery recipient does not silently update Customer identity. Each send rotates to a fresh immutable snapshot and invalidates the prior account-free link/session. Raw bearers are neither stored nor returned through history, provider results are recorded through a service-only boundary, and cooldown/daily limits constrain relay abuse. The exact migration is installed in Sandbox, Production, and Demo. Production uses the approved sensitive server-only Resend credential and passed a disposable Owner/provider/rotation/revocation smoke; provider acceptance was confirmed but the send-only credential cannot independently confirm inbox receipt. Demo received schema parity without a provider credential and remains intentionally fail-closed for outbound email.

Secure Estimate Acceptance for Not Connected Customers v1 adds one explicit recipient action to that existing request-free delivery session without creating a guest account or broader portal. An eligible recipient may accept only the exact immutable Estimate snapshot bound to the active delivery and digest-only session. The database locks the Estimate and snapshot inputs, rebuilds the canonical customer-facing snapshot, and requires exact document plus source-version correspondence before atomically recording one immutable `secure_guest` acceptance event and applying the existing `sent -> accepted` lifecycle transition. Duplicate and concurrent requests converge on the same event; stale, revised, expired, revoked, rotated, claimed, mapped, archived, inactive, malformed, and cross-tenant contexts fail closed. Contractor history shows the acceptance channel, timestamp, delivered version timestamp, and delivery recipient without exposing token, session, provider, or snapshot-hash internals. This is approval through possession of a secure guest delivery session, not verified identity or an electronic signature. Exact migration `servsync-secure-guest-estimate-acceptance.sql` (SHA-256 `a81a5f3b98c90a94ba36b1dab2e5a7e0ab7b9e756f783275e5a2ef4045a439b7`) is installed in Sandbox, Production, and Demo. All six function fingerprints, ownership/security/search-path/grant settings, the private forced-RLS acceptance table, constraints, indexes, and trigger matched across environments; Production and Demo preservation checks passed without creating acceptance fixtures. Authenticated lifecycle, role, stale-version, replay/concurrency, rotation/revocation, and Job-handoff evidence remains the completed Sandbox basis. Connected Customer authenticated approval, Home History, notifications, and manual accepted-Estimate-to-Job handoff remain unchanged. Signatures, stronger identity verification, payments/deposits, replies, SMS, report acknowledgment, decline/negotiation, and broader guest actions remain separate work.

Secure Guest Estimate Response v1 extends that exact recipient session with two distinct, bounded outcomes while preserving acceptance unchanged. `Request changes` requires a normalized 3-1,000 character message, records one immutable exact-delivery response, and returns the Estimate from `sent` to `draft` so the contractor can revise through the existing workflow and issue a fresh delivery; it does not edit pricing or create a conversation. `Decline` records an optional reason and applies the existing `sent -> declined` lifecycle only to that delivered version, without deleting records or preventing later work. Acceptance, change request, and decline share one Estimate advisory lock and are mutually exclusive per delivery; canonical snapshot reconstruction, source-version comparison, session/link locks, lifecycle checks, and a private forced-RLS/no-policy audit table keep stale, replayed, conflicting, foreign-tenant, claimed, mapped, archived, inactive, expired, rotated, and revoked actions fail-closed. Contractor history exposes only response type, recipient, timestamps, delivered version, and message. Exact migration `servsync-secure-guest-estimate-response.sql` (SHA-256 `ffeaa9ace8faf547d49c630fea920fc560ed67767e2653ceb2b272aeeba1c70d`) is installed and catalog-matched in Sandbox, Production, and Demo. Production rollback-only response probes and preservation checks passed without persistent fixtures; complete concurrent/race, role, and recipient UI evidence remains the validated Sandbox/exact-head basis. Production versus Demo remains `PASS WITH INTENTIONAL DIFFERENCES` with no unexplained supported-schema drift. These actions are secure guest responses, not verified identity, signatures, negotiation/chat, attachments, change orders, payment, or broader guest access.

Finalized Report External Email Delivery v1 reuses that recipient and provider architecture for the canonical filed customer-facing Job/inspection PDF. Owner, active Admin, and active Office users can send or resend one eligible finalized report for a Not connected Customer, use a delivery-only recipient override, review sanitized status history, and revoke recipient access. The private PDF itself is authoritative: the link snapshots its report/customer/property binding, storage identity, version, ETag, and size, and the server gateway revalidates the same fingerprint before proxying only that PDF through a digest-only 30-minute recipient session. No report metadata, Customer profile, property history, other work/documents, private notes, provider IDs, or raw token is exposed through the ordinary recipient/history surfaces. Connected Customer Home History/Documents and existing Estimate/Invoice delivery remain unchanged. Exact migration `servsync-finalized-report-external-email-delivery.sql` (SHA-256 `9a8b37bdb31ee01994123d362f6ee64681ca461f3778ee3ae652bafe62a7a843`) is installed and catalog-matched in Sandbox, Production, and Demo. Sandbox established report-specific authenticated/provider/recipient behavior; Production preserved its approved sensitive Resend runtime path and Demo remains intentionally provider-disabled. Provider acceptance is not represented as inbox-receipt confirmation. Draft/internal reports, report acknowledgment, signatures, payments, replies, SMS, broad guest access, Service Plans, and additional finalized document types remain separate work.

Stripe Connect Online Payments Foundation v1 extends the provider-neutral Invoice and offline-payment model. Contractor Owners use Stripe-hosted onboarding for a current controller-property connected account configured with fee payer `account`, payment-loss collector `stripe`, Stripe requirement collection, and the full Stripe Dashboard. Eligible Connected and Not connected Invoice recipients use Stripe-hosted direct-charge Checkout for the exact server-derived outstanding balance with card or ACH; ServSync sets no destination, transfer, `on_behalf_of`, or application fee, and the application fee remains `$0`. Signed Connect webhooks, not browser redirects, reconcile processing, settlement, failure, refund, and dispute changes into the existing Invoice balance so manual and provider payments remain one accounting model. Provider-backed Sandbox TEST acceptance proved an active card/ACH account, connected-account fee attribution, card success/failure, ACH asynchronous success/failure, webhook signature and retry behavior, mixed manual/online accounting, and reversal handling; the narrow provider-ID compatibility migration recognizes Stripe's current `py_` ACH identifiers without broadening other provider IDs. The exact provider-neutral schema is applied in Sandbox, Production, and Demo, but Production and Demo payment creation remain disabled by immutable server checks; no live key, account, Checkout Session, money movement, provider configuration, or launch permission is included. Job creation remains independent of deposit/payment state. Live activation, real contractor onboarding, receipts, refund/dispute operations, payout support, platform fees, and launch monitoring require a separate owner gate.

# 7. Current Build Audit Summary

The repository audit shows ServSync has progressed far beyond a simple prototype. It contains request, estimate, job, invoice, connection, permission, Discover, calendar, notification, document, report, reminder, and admin concepts. The recent PR #1-#11 sequence improved the core-loop path, Home History, manual reminders, SQL provenance, smoke-test expectations, calendar event details, and user-facing terminology. FB-011 mobile workflow polish has met its controlled-beta baseline with frontend-only responsive stacking, spacing, action hierarchy, mobile shell/navigation, PWA metadata, and smoke/checklist coverage; native apps, backend lifecycle changes, notifications, payments, and calendar sync remain future work. The provider-neutral integration foundation is preparation only: it gives future reviewed workers/RPCs a place to map external objects and queue idempotent side effects, but QuickBooks/accounting sync, payments/Stripe, calendar sync, email/SMS/push automation, e-signature, CRM import/export, and offline queues are not live. The risk is now less about finding the loop and more about beta readiness, end-to-end reliability, mobile density, and avoiding overpromising unbuilt automation.

| Area | Status | Beta concern |
| --- | --- | --- |
| Requests | Working and clearer after homeowner/contractor core-loop copy updates | Needs full request-to-invoice E2E coverage. |
| Estimates | Working with clearer accepted-estimate handoff, scope-aware rule-based draft builder, request-free Not connected Customer links, Production-active direct secure email delivery, exact-snapshot secure guest acceptance across Sandbox/Production/Demo, and Sandbox-validated Request changes/Decline responses pending Production/Demo rollout. | Continue broader recipient capability work without implying verified identity, electronic signatures, payments, negotiation/chat, or AI/pricing automation. Demo remains provider-disabled for email. |
| Invoices | Working, including invoice-to-Home-History filing v1 and clearer filed-invoice next action into Home History. Structured invoice type/order and estimate payment schedule foundations exist for future billing terms, contractor draft estimates can store structured schedule rows that appear on estimate PDFs and homeowner review, and accepted schedule rows can now create draft invoices through the approved RPC. FB-003B Request-Free Local Invoice Delivery Slice 1 is operational in Production for eligible unclaimed local customers through a same-origin gateway, bounded abuse controls, one-time Copy Link, and a fixed 30-minute digest-only recipient session. | Payment processing, anonymous PDFs, estimate/report links, claimed-account transition, provider delivery, contractor-side filing, replacement invoice behavior, automatic reminders, and accounting export are not live. |
| Jobs | Working but inspection-backed internally | Continue user-facing terminology cleanup without renaming schema casually. |
| Contractor local customers | The contractor-facing product presents one `Customer` category and shows connection as status: `Connected`, `Not connected`, `Invitation pending`, `Connection request`, or `Inactive`. `Homeowner` remains correct for the consumer account role, ownership, permissions, and homeowner-controlled data, while stable internal local-contact, connected-homeowner, route, RPC, and database identifiers remain unchanged. PR #371 merged that terminology/status foundation. PR #372 merged and deployed Unified Draft Customer Selection and Customer Profile Entry v1 in Production through normal merge commit `854372c8609a67d7de16dac33550762d9d750d74` and automatic `READY` deployment `dpl_3jkDZ6MKxyLZd9qCxCxi7YQGEqTY`: both Draft composers derive the existing canonical connected/local subject from one collision-safe Customer option, keep saved historical subjects in their original category, auto-select only one eligible property, require explicit choice among multiple properties, and route eligible connected/not-connected customer-profile initial work through neutral `Create Draft` without preselecting Estimate, Job, or Invoice. Exact-head authenticated Sandbox Preview evidence covers connected/local profiles, property variants, saved Draft restoration, Viewer denial, desktop/mobile, accessibility, and runtime health; bounded read-only Production smoke confirmed the merged generic composer and mobile layout, while the approved account's zero-customer state prevented those profile/property/Viewer scenarios from being repeated in Production. Contractor-created local customers exist for customers who are not yet on ServSync. The first local multi-property slice lets contractors save and view multiple local properties for one local customer and requires explicit property selection before starting property-specific work when more than one saved local property exists. Contractors can update unclaimed contractor-created local property labels, addresses, and private notes through a guarded RPC while blocking edits after the local property is linked to a homeowner profile. The local customer profile edit slice lets contractors update unclaimed local customer name, phone, email, and contractor-private notes only; claimed/homeowner-linked customer profiles remain homeowner-controlled, and edits to copied claim-invite fields revoke pending claim invites so stale links do not remain active. Customer Management Edit Boundary Correction v1 is merged and active in Production from PR #374 through normal merge commit `015e731b52c64168ceba5caa04669b2bc5fb798d`. Exact migration `servsync-customer-management-edit-boundary.sql` (SHA-256 `b86b3af7524d3d0122f97e21609569dfa316b7f87d2d2df213c5e73478d66427`) was applied transactionally to Production project `uqgtheclhxqlnjpfmheq`; automatic deployment `dpl_8m9W44zhmoCY636uWitHJcApaZCC` reached Production `READY` from the merge commit. All four affected Production functions matched the committed migration, remained owned by `postgres`, used `SECURITY DEFINER` with fixed `search_path=public`, granted execute only to `authenticated`, `postgres`, and `service_role`, denied `PUBLIC` and `anon`, and showed no unexpected overload or deployed drift. Sandbox authenticated validation established owner/admin/office profile and local-property mutation authority plus field tech, viewer, inactive, removed, cross-tenant, and anonymous denial; Production validation confirmed exact catalog/security correspondence, unchanged preservation fingerprints and business counts, healthy aliases, and 2/2 bounded authenticated read-only smoke without customer/property mutation. Customer-management authority no longer inherits general Job-write authority; tenant context precedes affected-row locking; generic foreign/unknown UUID behavior, claimed-record protections, invitation and claim-token containment, parent mappings, connected-homeowner ownership, and existing Draft/output capabilities remain intact. Contractor Local Customer Read/List Parity v1 is merged and active in Production from PR #376 through normal merge commit `fac9da09316e9ecc52649d9a181e7251a6efe421`. Its additive role-shaped summary boundary gives owner/admin/office eligible local-customer and property context, supplies redacted operational context for field-tech Draft/Job workflows without private/contact/claim data or customer/property mutation authority, limits Viewer rows to exact local subjects and properties linked to already-readable work, and exposes separate full management detail only to owner/admin/office. The frontend replaces the browser wildcard local-customer query, clears stale local rows, distinguishes loading/empty/unavailable/error/retry states, preserves connected-customer and saved-Draft behavior, keeps PR #374 mutation gates authoritative, and contains long customer/property labels at 390px. Exact migration `servsync-contractor-local-customer-read-list-parity.sql` (SHA-256 `0b90e4548ceec24e7bdd96a12ad9951f74b7ffc064f222a2f41b0e2ce109f41f`) was applied successfully to Production `uqgtheclhxqlnjpfmheq` on `2026-08-05` before merge. All three Production functions matched the audited definitions, ownership, `SECURITY DEFINER`, fixed search path, and restricted grants without RLS, direct-table privilege, customer/property/invitation data, environment, or configuration changes. Automatic deployment `dpl_H5hrgfw1evUtGbMJR5Rvtj6zG5QP` reached Production `READY` from the merge commit; both Production aliases returned HTTP 200. Full role/redaction behavior passed in Sandbox, while Production independently confirmed function correspondence and preservation plus bounded Owner desktop/mobile Customers behavior without data mutation or console/request failures. Production Field-tech and Viewer scenarios were not repeated because approved credentials were unavailable. Automatic Demo source deployment `dpl_CHNtsMaJNn3CUj3gi2AfCKLboj1u` was created from the merge commit, but the migration was not applied to Demo and Demo database support is not claimed. The claim-invite delivery hardening adds a guarded manual link/QR preparation boundary for contractor owner/admin/office users, keeps field tech/viewer users from preparing or revealing claim tokens, uses token-free ordinary reads, and keeps email/SMS/provider delivery excluded. FB-003A completes the explicit invitation-to-local-property membership model, exact contractor property selection, homeowner create-or-match review, atomic multi-property acceptance, legacy single-property compatibility, replay rejection, stale invalidation, connected-homeowner null safety, tenant isolation, and token containment. Controlled Production confirmation covered reviewed SQL/catalog security and public smoke, while authenticated Tests A-H passed only in Demo. The connected-homeowner property proposal flow remains a separate consent-first workflow. FB-003B Request-Free Local Invoice Delivery Slice 1 now provides a separate hash-only, document/property-bound Copy Link and same-origin gateway for eligible saved local invoices in Production. Recipient bootstrap immediately removes the original bearer from visible browser history and exchanges it for a digest-only fixed 30-minute HttpOnly session protected by server-only configuration, generic fail-closed responses, private database limits, and the Production Firewall. | FB-003A is complete and requires no further authenticated validation run. Full customer-management team-role parity remains incomplete even though the PR #376 read/list boundary is now active in Production. Contractor-local archive/restore is active in Production through PR #382; connected-customer organizational archive, deeper persisted subject unification, and broader customer-model maturity remain separate work. The unified Draft selector/profile-entry rollout does not merge persisted customer models or broaden authority. Service Plan parity, Demo SQL disposition, the inherited non-owner appointment-window 401, and the inherited ESLint 9 startup incompatibility remain separate work. Production has the PR #376 migration and validated read/list boundary; Demo does not, so Demo database support is not claimed. The bounded FB-003B local-invoice milestone is operational, while claim credentials remain separate and estimate/report delivery, PDFs, claimed-account transition, provider channels, reminders, approvals, payments, and replies remain future FB-003B slices. User-facing raw expected-replay RPC/JSON detail remains P3 polish under FB-036. |
| Home History | Homeowner-facing v1 is shipped on top of existing maintenance-log structures | Do not build a separate timeline table until beta feedback justifies it. |
| Reminders | Homeowner-only manual reminders v1 is shipped | No push/email/text alerts, recurrence, scheduler, or calendar sync yet. |
| Connections | Working | Needs contextual connection request messaging/media. |
| Permissions | Working for limited categories | Do not overclaim sharing categories. |
| Mobile | Responsive but dense | Needs core-loop mobile QA before broad beta. |

Current Demo note: rollout-time statements below that individual customer migrations initially reached Demo as source-only are retained as historical sequencing. The later controlled Demo schema-parity operation installed and authenticated the PR #374, #376, #378, #380, and #382 customer foundations plus the current Production-supported backend migration chain. Demo Presentation remains enabled, Draft rollout/UI gates remain off, request-free delivery server configuration remains absent, and Project Collaboration remains uninstalled; those intentional boundaries prevent a claim of full visible or operational Demo parity.

Admin/Office Customer Creation Parity v1 is merged and active in Production from PR #378 through normal two-parent merge commit `b31a3360cf95a61bda2eedca4d9943e2970cf728`, with base parent `18fdb53abf5e505e239d93ad9d12bd939dacbe9f` and reviewed-head parent `3b4165f3f4c01408dfe8a58e500ad88674d8cd00`. Exact migration `servsync-admin-office-customer-creation-parity.sql` (SHA-256 `ce3fc2b8e79bab14790c27577e66d2e7d6c0a27cd4c1cf1ffe8856877531b78b`) was applied successfully to Production project `uqgtheclhxqlnjpfmheq` from `2026-08-05T18:42:11Z` through `2026-08-05T18:42:12Z`. The single deployed RPC matched the exact audited body, remained owned by `postgres`, used `SECURITY DEFINER` with fixed `search_path=public`, granted execute only to `authenticated`, `service_role`, and `postgres`, denied `PUBLIC` and `anon`, and showed no drift or RLS, direct-table ACL, customer/property data, environment, or configuration change. It derives contractor identity from the authenticated session, accepts no contractor, customer, property, or parent identifier, atomically creates one contractor-managed customer and initial property for Owner, active Admin, or active Office, returns an explicitly allowlisted response, and keeps Field Technician and Viewer denied. Full role enforcement, paired creation, controlled-directory visibility, and Admin/Office creation-to-Draft behavior passed in Sandbox. Production confirmed exact function correspondence and preservation; Owner desktop/mobile creation controls rendered correctly, the form was canceled without submission, and no Production customer or property was created or modified. Production Admin, Office, Field Technician, and Viewer scenarios were not repeated because approved credentials were unavailable, so Sandbox remains the evidence basis for those roles. Automatic Production deployment `dpl_Ch4vXbVUvCfCqCipwyhKFLb6x2iY` reached `READY` from the merge commit, and both Production aliases returned HTTP 200. Automatic Demo source deployment `dpl_A7ZPDMyhUyR1Nt5LExZJ4m7nn3fa` received source only; the migration was not applied to Demo and Demo customer-creation parity is not claimed. PR #374 mutation restrictions, PR #376 read/list boundaries, and connected-customer, claim, invitation, property, Draft, and output-authority behavior remain unchanged. Contractor-local archive/restore is active in Production through PR #382; Demo migration disposition, broader customer-model maturity, the inherited appointment-window 401, and ESLint 9 repair remain separate active work; this bounded slice does not complete customer-management parity or customer-model consolidation.

Contractor-Local Customer Direct-Table Privilege Cleanup v1 is merged and active in Production from PR #380 through normal two-parent merge commit `e8767c8bf795f46eb7fbb76aa98a8d0d14bc3f46`, with base parent `e1d79edc83c80296905ada7080fa9ad729ac9f84` and reviewed-head parent `6de5a38e074ba9135bb09888a1753f4f543325b8`. Exact migration `servsync-contractor-local-customer-direct-table-privilege-cleanup.sql` (SHA-256 `c01332b1422c136cdb6fed5d580c7deacf5cf49ac1debfaf02be06f7e3a5cf64`) was applied successfully to Production project `uqgtheclhxqlnjpfmheq` from `2026-08-05T20:46:34Z` through `2026-08-05T20:46:36Z`. On `contractor_local_contacts` and `contractor_local_homes`, `PUBLIC`, `anon`, and `authenticated` retain no table-level or column-level privileges; trusted `service_role` retains only `SELECT`, `INSERT`, `UPDATE`, and `DELETE`, with `TRUNCATE`, `REFERENCES`, `TRIGGER`, and other unintended privileges denied, and `postgres` ownership preserved. RLS, existing policies, controlled RPC definitions and grants, default ACLs, schema, columns, constraints, indexes, and business data remained unchanged, and no archive, restore, deletion, or cleanup RPC was added. Owner and every other browser role can no longer directly read or mutate raw contractor-local customer/property rows; supported behavior continues through the controlled directory, management-detail, creation, profile/property update, claim, invitation, Draft, Job, estimate, invoice, inspection, and calendar boundaries. PR #374 mutation restrictions, PR #376 role-shaped reads, and PR #378 Owner/Admin/Office creation parity remain intact. Anonymous and authenticated Owner direct CRUD denial each passed 8/8, controlled Owner RPC checks passed, and authenticated Production read-only smoke passed 2/2 for homeowner navigation and contractor Owner Customers/Jobs navigation without Production data mutation. Field Technician and Viewer Production UI paths were not repeated because approved credentials were unavailable; exact Production ACL verification and completed Sandbox role validation remain the evidence basis. Automatic Production deployment `dpl_EYunKyFyZP7oJumj9E3o4gfER1fd` reached `READY` from the merge commit, and both Production aliases returned HTTP 200. Automatic Demo source deployment `dpl_oUtbS5W8uvAUq6fhZewECknCkhxs` received source only; the migration was not applied to Demo and Demo database support is not claimed. This slice hardens only these two existing tables and does not automatically protect future sensitive tables. Contractor-local archive/restore is active in Production through PR #382; Demo customer-management migration disposition, broader PostgreSQL default-ACL policy, broader customer-model maturity, the inherited appointment-window 401, and inherited ESLint 9 repair remain active and separate.

Contractor-Local Customer and Property Archive/Restore v1 is merged and active in Production from [PR #382](https://github.com/bencatchot/ServSync-App/pull/382) through normal two-parent merge commit `67df8c6b41b903b50c4dac3a50921ada107b463d`, with base parent `145cb56b1629e867967e36ce15f2900e6c63e8ef` and reviewed-head parent `59502ec05ae025e6d2e2a183997bade47cd7d559`. Exact migration `servsync-contractor-local-customer-property-archive-restore.sql` (SHA-256 `762c6fdd8b1dacabb70730c7257691f959ebfd07cf67d2688b97756015a46c17`) was applied successfully to Production `uqgtheclhxqlnjpfmheq` from `2026-08-06T12:14:42Z` through `2026-08-06T12:14:44Z`. Production has no `public.projects`; the corrected project-optional migration installed the complete non-project lifecycle without creating a project table, trigger, guard function, or Project Collaboration feature, while archive impact and historical project context retain stable zero/empty response values. Sixteen project-absent function definitions matched the audited migration with one overload each, `postgres` ownership, `SECURITY DEFINER`, fixed `search_path=public`, and intended grants; 11 non-project lifecycle/assignment triggers, six indexes, five explicit constraints, private forced-RLS lifecycle events, and existing contractor-local direct-table restrictions are installed. Owner, active Admin, and active Office may archive and restore eligible unclaimed contractor-local records; lower, inactive, homeowner, anonymous, unauthenticated, and cross-tenant identities fail closed, and claimed/mapped records remain excluded. Archive preserves IDs and all existing operational work and historical labels, does not mutate operational lifecycle states, revokes pending invitations without token revival, keeps independently archived properties archived after customer restore, blocks property restore beneath an archived parent, and prevents stale or forged new assignments. Production counts and identifier digests remained stable at 3 local contacts, 12 local homes, 1 claim invitation, 7 Drafts, 9 Jobs/inspections, 22 estimates, 11 invoices, and zero archived contacts, archived homes, or lifecycle events; ACL, RLS, and policy fingerprints remained unchanged apart from the approved additive archive schema/functions. Full role, lifecycle, invitation, race, historical-label, and assignment enforcement remains grounded in authenticated Sandbox evidence. Production independently verified exact migration/catalog correspondence and preservation; Contractor Owner desktop/mobile impact dialogs rendered `Projects 0` and were canceled without invoking archive/restore or changing business data, with zero console or server failures. Automatic Production deployment `dpl_7hmJGtxh7DicVK4JhNPWh9tsXXh3` reached `READY` from the merge commit and both Production aliases returned HTTP 200. Automatic Demo deployment `dpl_5kLQC2jmMjndqxuosUPG8oePf22P` received source only; no Demo migration or Project Collaboration installation occurred, and Demo archive/restore support is not claimed. Connected-customer organizational archive, Demo migration disposition, Project Collaboration disposition, hard deletion/retention, bulk archive, broader default-ACL policy, broader customer-model maturity, Service Plan parity, external delivery/guest actions, the non-owner appointment-window 401, and inherited ESLint repair remain separate work.

# 8. Homeowner Experience

The homeowner experience should feel lightweight. Most homeowners will likely have one property, so the app should not constantly force a multi-property mental model. Multi-property support should exist, but the default user experience should be simple for a single-property homeowner.

- Homeowner can create an account and add basic home/property info.
- Home Setup should grow from structured property data rather than floor-plan/CAD tooling. The lightweight Home Setup guide shell, homeowner Rooms UI, room-linked reminders/documents, Room Detail, Home Map v1, and Assets & Systems v1 are live as homeowner-owned organization tools, now centered on a dedicated full-page Home Map Builder as the primary room-management experience. Owner/admin home managers can manage room basics, simple not-to-scale map boxes, and safe asset/system basics; shared admins can manage where existing home-management semantics allow it; shared member/viewer roles can view non-sensitive room and map basics read-only where surfaced, while asset browsing stays manager-only in v1. Room and asset notes remain manager-only, documents remain owner-scoped, and Home Map document/reminder counts stay owner/admin-only. Home Map v1 is a simple box-based room layout with optional display-only rough dimensions and is not CAD, a measured floor plan, LiDAR scan, floor-plan generation, or 3D modeling. The UX treats the room as the map object, lets homeowners add new rooms/hallways directly to the builder, add existing unmapped rooms without duplicating room records, makes room boxes roughly proportional to entered length x width, supports hallways as normal room/map spaces without a separate hallway model, autosaves move/resize changes, flushes pending saves before layout reload actions, and now keeps the canvas dominant with a minimal header, side toolbar, simplified room boxes, secondary room-list overlays, and room details in a drawer/bottom-sheet surface instead of a persistent canvas-crowding panel. The Home Map Draft foundation adds SQL/RLS/RPC boundaries for contractor-created, service-request-scoped map proposals: contractors may draft proposed map rooms/layouts through RPCs for authorized active homeowner workflows, but homeowner managers must approve before anything writes into the permanent Home Map. The first contractor UI slice surfaces a request-scoped blank draft builder inside contractor Service Requests, using only draft tables/RPCs for create, add/update room, submit, and revoke actions. It does not add homeowner review UI, contractor direct access to permanent Home Map tables, contractor asset browsing, document/reminder/storage expansion, or permanent map import. Ceiling height remains future structured data requiring a separately approved SQL-backed slice if needed. L-shaped and other multi-section rooms require separately approved SQL/RLS work, likely room layout sections, because the current UI treats `home_room_layouts` as one active layout row per room. Contractor direct Home Map/asset visibility, homeowner draft review UI, Key Home Locations, sensitive access fields, serial numbers, floor-plan upload/recreation, `home_map_objects` such as doors, windows, stairs, counters, islands, cabinets, closets, appliance markers, and utility markers, photo/report-job media room tags, inspection/job/estimate/invoice map links, and storage/entitlement changes remain future separately approved work.
- Future household/shared homeowner access should be home-level rather than account-wide. FB-030 Slice 1A begins this direction with an additive `home_memberships` SQL/test foundation, owner/admin/member/viewer roles, primary-owner backfill, and helper functions for later RLS work. Slice 1B adds guarded invite/accept/decline/revoke RPCs and audit events for home memberships without adding UI, email/SMS/push delivery, or shared access to existing homeowner records. Slice 1C adds a backend-only pending email invite foundation so future homeowner UI can invite household members by email without exposing raw auth user UUIDs or revealing whether an address already has a ServSync account. Slice 1D adds the first homeowner-facing Home Access UI shell in Properties, wiring pending email invite create/list/revoke/accept/decline actions through existing RPCs and making clear that email delivery and shared dashboard records are not enabled yet. Slice 1E adds a limited read-only shared-home shell RPC and separate `Homes shared with me` UI section that shows only safe label/city/state/role/status fields for accepted admin/member/viewer memberships, without full address, storage, Home History, contractor connections, or shared service/work/financial/document/message records. Slice 1F-A adds an additive address-aware shared-home shell RPC foundation for future UI use: active admin/member memberships may receive street/zip fields, viewer memberships remain city/state only, and the existing Slice 1E UI/RPC remains unchanged until a later UI slice. Slice 1F-B migrates the separate `Homes shared with me` UI to the address-aware RPC after production rollout, showing street/zip only when returned for admin/member roles and city/state-only limited-location copy for viewer results, while keeping shared homes out of owner selectors and shared records disabled. Slice 1G-A adds a SQL/RPC-only read-only shared reminder shell for accepted admin/member/viewer memberships, returning only open reminder title/due date/status/home label/role fields and no notes, linked records, or reminder mutation. Slice 1G-B adds the UI display for those open shared reminder shells inside the separate `Homes shared with me` cards, keeping reminders read-only and excluding notes, links, owner dashboard selectors, and other shared record surfaces. Slice 1H-A adds the SQL/RPC delivery foundation for Home Access email invites: delivery metadata, 30-day invite expiry defaults, a sanitized delivery-preparation RPC that currently returns delivery disabled, and an internal-only delivery-result recorder for future worker use. Slice 1H-B adds a dedicated Home Access invite email Edge Function scaffold that calls the 1H-A prepare/result RPC contract and supports disabled/mocked provider behavior. Slice 1H-C and Slice 1H-D deploy that function to sandbox and production with delivery disabled, no provider secrets, and no real email/SMS/push delivery. Slice 1H-E wires the Home Access invite UI to call the deployed function after invite creation using only the invite ID, while keeping copy explicit that the invite was saved but email delivery is disabled and the person was not emailed yet. Slice 1H-F-A adds a private DB runtime setting contract so invite delivery remains disabled by default everywhere, while an approved operator can explicitly enable the DB side in sandbox for provider testing without changing UI, Edge Functions, storage policies, or shared record access. Future slices must opt service requests, estimates, invoices, jobs, documents, reminders, notifications, storage, and workflow records into shared access one surface at a time.
- Homeowner can browse Discover and contractor profiles.
- Homeowner can request a connection with a contractor and include a message, selected property context, and permission choices. Photos/media should come later after storage and RLS handling are designed safely.
- The homeowner Contractors area should act as the relationship/discovery hub: find contractors, manage permission-based connections, invite known contractors, and then start service requests when ready. The Service Requests area should stay focused on work intake and request tracking.
- Homeowner can grant permission to share implemented categories: contact info, home overview, address, preferred vendors, photos.
- Homeowner can submit service requests, review estimates, approve/decline, view invoices, file eligible invoices to Home History, use filed-invoice next actions to return to Home History, create manual reminders, and access home service history.
- Homeowner should be able to communicate with a contractor without starting a service request.

# 9. Contractor Experience

The contractor experience should be practical, not bloated. The contractor should quickly understand: how do I get seen, receive better-context requests, communicate, estimate, perform the job, invoice, and keep the customer relationship organized?

- Contractor creates a profile with business details, service area, trade type, and future Discover content.
- Contractor can be found by homeowners in Discover/search/feed experiences.
- Contractor receives contextual connection requests with homeowner message, selected property context, and permission choices. Photos/media should come later after storage and RLS handling are designed safely.
- Contractor can reply within homeowner-initiated allowed communication threads without forcing every conversation into a service request, but a connection alone should not grant unrestricted contractor-initiated messaging.
- Contractor can convert serious needs into requests, estimates, jobs, invoices, and service records.
- Contractor-created local customers can now hold multiple contractor-private local property records, and property-specific jobs/estimates/invoices should use explicit local property selection when more than one saved local property exists. Contractors can edit unclaimed local customer/property details through guarded RPCs, while claimed profiles remain homeowner-controlled. Contractors can also edit unclaimed local customer name, phone, email, and contractor-private notes only; profile changes that affect copied claim-invite information revoke pending claim invites instead of leaving stale links active. Claim-invite manual link/QR sharing remains guarded, token-contained, and provider-free. FB-003A's completed multi-property claim workflow remains separate from operational documents, and connected-homeowner property proposals remain a separate consent-first workflow. FB-003B Request-Free Local Invoice Delivery Slice 1 lets an authorized owner/admin/office user issue one eligible local invoice and create, rotate, or revoke an expiring document-specific Copy Link in Production. Not-Connected Customer Capability Parity v1 provides the equivalent bounded immutable-snapshot link model for eligible Estimates in Sandbox, Production, and Demo, preserving the richer authenticated Connected Customer approval experience. Secure Estimate Email Delivery v1 reuses that model: the contractor confirms a delivery-only recipient, the server rotates and emails one expiring document-scoped link, and sanitized attempt history distinguishes sending, sent, and failed outcomes. Its exact migration is installed in all three environments; Production's actual provider path is enabled and validated, while Demo remains provider-disabled. SMS, finalized-report delivery, anonymous PDFs, acknowledgments/acceptance, signatures, payments, replies, Service Plans, and broader guest access remain separate. Raw expected-replay RPC/JSON detail remains P3 polish under FB-036.
- Contractor should see clear next actions instead of hunting across tabs. Recent cleanup work made request-to-estimate, accepted-estimate-to-job, completed-job-to-invoice, and service/checklist terminology clearer without changing stored status values or internal database names. Accepted estimate job handoff now sends dashboard `need jobs` actions directly to the Approved estimates view and keeps Create/View Job primary over secondary estimate actions. Saved/focused estimate records also support an estimate-led `Create invoice from estimate` action that opens the existing editable invoice composer for draft/sent/accepted estimates without requiring an ordinary service contractor to create or visit a job first; this does not add invoice types, create-another-invoice behavior, payment collection, Home History filing, or SQL/RLS/RPC changes.

# 10. Discover Marketplace Strategy

Discover is the platform-wide differentiator because it gives contractors exposure and gives homeowners an organized alternative to scattered local recommendation threads. It should not become another generic contractor search page. It should feel like an organized local contractor feed and profile network.

| Discover principle | Direction |
| --- | --- |
| Primary value | Exposure for small contractors and a better starting point for homeowners. |
| Homeowner need | Find relevant local contractors with enough context to take the next step. |
| Contractor need | Be visible without relying only on paid ads or word-of-mouth chaos. |
| Main CTA | Connect. |
| Secondary CTAs | Request service, view profile, save contractor, ask question/message. |
| Content strategy | Contractor posts/feed, practical updates, service examples, maintenance tips, before/after content later when real. |

The Discover tab should not show a generic property selector at the top for single-property homeowners. It should focus on contractor exposure and recent contractor content. Contractor search belongs primarily in a contractor search/profile context, while Discover should feel more feed-like.

FB-026 review/referral work is now started/partial rather than a blank slate. ServSync review capture already exists for eligible completed ServSync work, but public ServSync review averages, counts, kudos, and snippets are paused during beta until moderation/public-display policy is approved. Contractor-provided external review links remain separate from ServSync reviews. Review writes are intended to flow through guarded RPC logic, with direct browser table grants hardened for the review table. The moderation foundation now defaults reviews to pending moderation and provides guarded platform-admin backend RPCs plus a minimal internal platform-admin review moderation UI for status and note updates. Admin approval records moderation status only and does not publish reviews publicly yet; approved-only public review display is not live. Homeowner review form copy now treats optional name/location fields as future display details that may be reviewed before any future public display. The contractor-to-contractor referral foundation adds production-applied backend SQL/RLS/RPC boundaries for admin-trackable `contractor_refers_contractor` referral invite leads, the first contractor submit UI lets eligible contractor owner/admin/office users submit another contractor's details for ServSync team follow-up only, and the first platform-admin tracking UI lets admins manually review status, notes, outreach count, and follow-up dates. It does not add email delivery, rewards, billing, contractor team access, contractor profiles/accounts, public landing pages, contractor-to-contractor permissions, or workflow automation. Public rating expansion, homeowner recommendation/referral product decisions, referral outreach/product decisions, Google review handoff, badges, awards, paid ranking, and pay-to-play placement remain future work and must not be implied as live.

# 11. Connection System and New Connection Request Flow

A ServSync connection is not just a message. It is an app-level relationship between homeowner and contractor. It enables permission-based sharing and makes future requests, estimates, jobs, invoices, and home records easier to manage. This is one of the strongest differentiators in the product.

| Approved direction<br>When a homeowner clicks Connect on a contractor profile, they should be shown a contextual connection request flow where they can select one or more properties, choose per-property permissions, copy permissions from another selected property, add a message, and send the request. The contractor should receive context instead of a blind connection request. Photos/media are intentionally deferred from the first implementation slice until storage, media access, and RLS handling are designed safely. |
| --- |

## 11.1 Connection Model

ServSync should keep one homeowner-contractor connection as the relationship model. Do not create separate contractor customer records or separate connection rows per property. A homeowner may share one or more properties with a contractor under the same connection, and the contractor should see one connected homeowner/customer with shared properties listed underneath that relationship.

This keeps the relationship simple while still supporting multi-property homeowners:

- One connection represents the relationship between the homeowner and contractor.
- Shared properties live under that one connection.
- Each shared property can have its own property-specific permissions.
- The contractor customer list should not duplicate the same homeowner once for each shared property.
- Contractor-side property selection should be limited to explicitly shared properties or to properties tied to another already allowed homeowner-initiated workflow.

## 11.2 Contextual Connection Request Fields

| Connection request field | Purpose |
| --- | --- |
| Properties | Lets the homeowner choose one or more properties to share under the connection. |
| Per-property permissions | Lets the homeowner control what information is shared for each selected property. |
| Copy permissions from property | Lets the homeowner copy one selected property's permissions to another selected property. |
| Message box | Lets homeowner ask a question or explain why they want to connect. |
| Request Connection button | Sends contextual request to contractor. |

- The request should not automatically become a service request unless the homeowner chooses that path.
- The contractor should be able to accept, decline, or respond/ask for more information depending on workflow design.
- After connection, a future general communication thread may be added in addition to request-specific messaging, but that remains a separate workflow decision after contextual connection requests and must preserve homeowner initiation/control.
- Optional photos/media should be treated as a later phase, not part of the first contextual connection request implementation slice.

## 11.3 Contractor Customer View After Connection

The contractor-side customer view should continue to show one connected homeowner/customer. Shared properties should appear under that customer relationship and route to existing work areas rather than creating a separate open-work management system.

Suggested customer actions:

- View shared profile.
- View shared properties.
- View open Jobs/Estimates/Invoices.
- View Homeowner History.

The open Jobs/Estimates/Invoices shortcut should be customer-specific and should route to existing records. If open items exist, the contractor should select the relevant estimate, job, or invoice and open the existing location. If no open items exist, use the empty state: "No open jobs, estimates, or invoices for this homeowner."

The Homeowner History shortcut should reuse existing completed/closed jobs, finalized reports, and closed/billed records where possible. It should not expose the full homeowner Home History unless a future explicit permission category is approved. History must be limited to records visible to that contractor through shared properties or another allowed workflow.

## 11.4 Contractor Guardrails

The connection should not become a broad permission to initiate any workflow on behalf of the homeowner.

- Contractor cannot create a service request for the homeowner.
- Contractor cannot send an estimate unless tied to a homeowner-initiated request or another existing allowed workflow.
- Contractor cannot create a job unless tied to an accepted estimate or another existing allowed workflow.
- Contractor cannot initiate general messaging unless the homeowner started the communication.
- Contractor can reply only where the homeowner has initiated communication through an allowed workflow.
- Contractor property selection for jobs/work must be limited to explicitly shared properties or properties tied to an already allowed homeowner-initiated workflow.

# 12. General Messaging Without Service Request

Currently, communication is mostly tied to service requests or support contexts. A homeowner may want to ask a contractor a question without formally requesting service. This is a real use case and should be added to the roadmap as connection-level communication.

| Messaging type | Description | Priority |
| --- | --- | --- |
| Connection request message | Message/photos included before connection is accepted. | High beta differentiator. |
| Connection-level thread | General homeowner-contractor conversation after connection. | High after connection request flow. |
| Request-specific messaging | Messages tied to a service request/job context. | Already exists, keep. |
| Support messaging | User-to-ServSync support or admin context. | Already exists separately. |

Guardrail: messaging should not become chaotic. The UI should make clear whether a message is general, attached to a connection request, or attached to a specific service request/job.

# 13. Permissions and Shared Information

Permission-based sharing should remain honest and limited to what is implemented. Current implemented categories are contact info, home overview, address, preferred vendors, and photos. Marketing and onboarding should not imply that estimates, invoices, reports, requests, notes, or full job history are automatically shared through permissions unless those become explicit categories later.

| Implemented category | Public explanation |
| --- | --- |
| Contact info | Basic contact details used so the contractor can communicate. |
| Home overview | High-level home/property information useful for service context. |
| Address | Service location information. |
| Preferred vendors | Known vendor contacts saved by the homeowner. |
| Photos | Homeowner-provided photos relevant to the property or issue. |

- Future permission categories could include documents, service history, reports, estimates, invoices, or notes, but these should not be marketed as current unless implemented.
- Connection permissions should be clear before, during, and after the connection request.
- Homeowners should be able to understand and revoke/modify access where supported.

## 13.1 Per-Property Permission Direction

For multi-property connection sharing, property-specific permissions should use only currently supported app categories unless a future approved implementation adds more categories:

- Home overview.
- Address.
- Preferred vendors.
- Photos.

Contact info should remain connection-level unless a later audit proves the data model needs otherwise. Contact info belongs to the homeowner-contractor relationship, not to a specific property. If a contractor can contact the homeowner, that permission should be understood as relationship-level access.

Do not add or market new permission categories as live until they are implemented and covered by RLS/test coverage.

## 13.2 Permission Copy UI Direction

The preferred MVP control for multi-property permission setup is:

Copy permissions from: [Property dropdown] [Apply to this property]

This lets a homeowner configure one selected property and copy those settings to another selected property without hiding the fact that each property can have its own access rules. A top-level "apply same permissions to all selected properties" shortcut may be considered later, but the copy-from-property control is the preferred first implementation because it keeps the per-property model explicit.

## 13.3 Existing Active Connection Migration Direction

The SQL/RLS implementation must make an explicit migration or fallback decision for existing active connections. Recommended beta-safe direction:

- Preserve existing visibility behavior by seeding shared-property rows for homes that are already visible under the current connection-level permission model.
- Do not silently expand contractor access beyond the current model.
- Add a future homeowner editing/re-confirmation path so property access can be reduced or refined.
- Avoid mutating historical connection data without a narrow reviewed SQL plan and explicit SQL application approval.

# 14. Pricing Strategy

The pricing strategy should support contractor adoption while preserving room to grow. Early pricing should not pretend the product is as mature as major competitors, but it should not be free forever for contractors. Homeowners should stay free for core use because homeowner growth creates contractor value.

| Plan concept | Early price idea | Included value | Notes |
| --- | --- | --- | --- |
| Free homeowner | Free | Home profile, service requests, connections, estimates/invoices, history basics. | Potential storage limits later. |
| Contractor Basic | ~$5/mo | Discover presence, profile, simple communication/connection features. | Useful to get contractors on the platform early. |
| Contractor Premium | ~$20/mo after 30-day free trial | Full contractor workflow: requests, estimates, jobs, invoices, calendar, records, tools. | Initial beta/early launch target. |
| Additional users | First two at $20/mo each; additional users ~$10/mo later | Multi-user team access. | Likely later; may change if team tools become stronger. |
| Future storage add-on | TBD | Expanded document/photo/PDF storage. | Free accounts minimal storage; paid accounts larger storage. |

The document should explicitly state that pricing will likely change as ServSync proves reliability, gains capability, and supports more complex contractor workflows. Near-term price increases are not planned, but long-term pricing should reflect proven value.

- Keep contractor pricing cheaper than major competition early on.
- Avoid enterprise-tier assumptions until the product truly supports teams of 3-5+ with admin, permissions, dispatch, and reporting depth.
- Consider keeping Basic low-cost to seed Discover supply, but avoid giving away expensive storage or advanced tools for free.

# 15. Beta Strategy

ServSync should be positioned as beta because the job workflow and communication patterns will evolve based on actual user feedback. Beta should not mean unreliable; it should mean early access to a useful product that is still being refined with honest feedback.

| Beta principle | Execution |
| --- | --- |
| Small initial group | Start with 2-3 friendly beta users before inviting a broader group. |
| Honest positioning | No fake users, fake metrics, fake testimonials, or exaggerated claims. |
| Focus on core loop | Do not chase new features until request -> estimate -> job -> invoice -> history is reliable. |
| Feedback emphasis | Ask contractors where the job workflow feels wrong or too slow. |
| Marketplace learning | Measure whether Discover/connection creates real contractor interest. |

Current beta-readiness notes:

- Required authenticated production read-only smoke is configured for dedicated homeowner and contractor owner smoke accounts; the latest run passed preflight plus 2/2 read-only sign-in/navigation tests without production mutation, SQL, settings changes, or manual deploy.
- Unauthenticated production public smoke is formalized as a read-only check for `https://servsync.app` public/legal/trust routes and must not sign in, create users, mutate production records, apply SQL, change settings, or deploy manually.
- FB-020 Slice 1D adds a no-secret local production smoke credential readiness check so operators can see whether approved `PROD_SMOKE_*` variable names are present without printing values, signing in, validating credentials, or contacting Supabase.
- The first authenticated production smoke scaffold is read-only, uses only approved `PROD_SMOKE_*` credentials, requires `TEST_APP_URL=https://servsync.app`, and now has passing required homeowner/contractor owner sign-in/navigation evidence. Optional role credentials and optional stable smoke record IDs remain future/not configured; mutation smoke remains a separate future tier.
- Preview/sandbox should remain the default place for authenticated testing.
- Full end-to-end coverage for the core loop remains future work.
- Mobile visual QA remains important because contractor and homeowner cards can become dense.
- Local Vercel CLI account/project mismatch is a non-blocking tooling cleanup item.
- FB-020 now tracks security, records reliability, backup/restore, storage, dependency security, and scale readiness as a cross-cutting workstream before broader beta/public go-live. This does not mean those readiness items are complete; they require separate audited implementation and verification.
- FB-020 Slice 1C expands the sandbox security catalog for newer workflow/storage surfaces and hardens two authenticated-only homeowner lifecycle RPC grants in sandbox and production.
- FB-020 Slice 1F adds template-only capture formats for applied-SQL ledger entries, production SQL rollout evidence, non-production restore drill results, storage backup/readiness worksheets, and Edge Function/env/secret restore notes. This does not mean a restore drill has been run or that production backup/PITR/storage restore has been verified.
- FB-020 Slice 1G adds local `.gitignore` guardrails for generated backup, restore, export, query-output, storage-export, and ops artifacts. This reduces accidental commit risk only; it does not mean a restore drill, PITR verification, or storage restore verification has been completed.
- FB-020 Slice 1H adds a docs-only non-production restore drill preflight plan covering required approvals, isolated target-environment rules, data handling, verification preparation, artifact handling, cleanup, and evidence capture. This does not mean a restore drill has been run or that backup/PITR/storage restore readiness has been verified.
- FB-020 Slice 1I adds a docs-only non-production restore drill operator checklist for future go/no-go review. This does not mean a restore drill has been run or that backup/PITR/storage restore readiness has been verified.
- FB-020 Closeout B completed the first non-production database-only schema restore drill against an isolated throwaway Supabase project. Public schema restore and sanitized shape verification passed, but full data restore stopped because public rows reference `auth.users` and auth-user restore was not approved. Storage restore, production data, backup/PITR verification, and full data/auth restore remain future approval-gated work.
- FB-020 Closeout C marks the controlled private beta baseline met for the current beta stage. The baseline is supported by production public smoke coverage, required production authenticated read-only smoke for homeowner and contractor owner accounts, no production mutation during smoke testing, restore drill preflight/operator docs, backup/restore evidence templates, backup/restore artifact `.gitignore` guardrails, the first non-production database-only schema restore drill, schema restore verification matching 66 public tables, 179 public functions, and 66 RLS-enabled public tables, cleanup of the throwaway restore target and local artifacts, and no committed secrets/artifacts. This does not mark FB-020 fully complete: full data/auth restore, storage restore, backup/PITR verification, storage-object backup/restore strategy, optional production smoke roles, optional stable production smoke records, public go-live readiness, and paid-subscription readiness remain follow-up gates.

# 16. Roadmap Priorities

The roadmap should stay disciplined. The app already has many features. Recent cleanup work made the core loop clearer and shipped v1 Home History, invoice-to-Home-History filing, homeowner manual reminders, SQL provenance cleanup, calendar event details, and terminology improvements. The next work should harden beta readiness, improve test coverage, and add marketplace/connection differentiators in small audited branches.

| Priority | Workstream | Why it matters |
| --- | --- | --- |
| 1 | FB-020 security, records reliability, backup/restore, storage, and scale readiness | Keeps privacy, data durability, recoverability, dependency security, and 1,000-user readiness ahead of broader beta/public go-live claims. |
| 2 | Beta readiness and release checklist | Confirms the cleaned-up core loop is dependable enough for friendly beta users. |
| 3 | Full E2E core-loop coverage | Protects request -> estimate -> approval -> job -> invoice -> Home History -> reminder behavior. |
| 4 | Mobile core-loop QA | Contractors will use this in the field, and recent cards/actions need real viewport review. |
| 5 | Production smoke account and record plan | Maintains safe authenticated production smoke without using personal or fragile accounts, and leaves optional role credentials/stable smoke records as separately approved follow-ups. |
| 6 | Contextual connection request with message, selected properties, and per-property permissions | This strengthens the main differentiator. Photos/media should follow later after storage/RLS design. |
| 7 | General connection-level messaging | Allows questions without forcing service requests. |
| 8 | Discover feed strategy audit | Keeps marketplace work grounded in real contractor posts/photos and homeowner discovery needs. |
| 9 | Dashboard and notification accuracy | Builds trust and reduces confusion. Notification automation remains future-facing. |
| 10 | Beta onboarding and feedback capture | Turns early users into product direction. |
| 11 | Payments/Stripe, QuickBooks, push/email/text automation, native mobile apps, and full calendar sync later | Important, but after the core loop and beta operations are reliable. Estimate payment schedules should stay provider-neutral; future QuickBooks IDs should live in an external accounting mapping layer rather than core estimate schedule rows. |

SQL provenance note: tracked replacements for estimate-to-job support and home-specific inspection templates are now in main. If old loose SQL files such as `servsync-estimate-to-job.sql` or `servsync-home-specific-templates.sql` are encountered outside the repo, do not apply them directly; use the reviewed tracked SQL patches instead.

# 17. Codex Operating Rules

Codex should be used for repository audit, implementation planning, code changes, tests, and PRs. It should not be handed this master plan with permission to change the app broadly. Every implementation task should be narrow, approved, and reviewed.

- Codex must audit first when the task could affect multiple areas.
- Codex must not change code without explicit approval.
- Codex must report files changed, checks run, and risk level.
- No production deployment or merge to main without explicit approval.
- For authenticated Playwright testing, Codex should load .env.test.local, verify required env names exist without printing values, and use the branch-specific TEST_APP_URL.
- Codex prompts should be stored separately from this master plan when they are implementation-specific.

# 18. GitHub Storage Plan

The master plan should be stored in GitHub so it stays near the code and can be referenced during product work. Since Word documents are not ideal for code agents, use both a Word copy and a Markdown companion when possible.

| File | Recommended path | Purpose |
| --- | --- | --- |
| ServSync_Master_Plan_v1_0.docx | docs/strategy/ServSync_Master_Plan_v1_0.docx | Human-readable formatted plan. |
| servsync-master-plan-v1.md | docs/strategy/servsync-master-plan-v1.md | AI/Codex-readable text version. |
| roadmap.md | docs/strategy/roadmap.md | Short task roadmap for active work. |
| beta-test-plan.md | docs/beta/beta-test-plan.md | Manual beta testing checklist. |
| codex-prompts/ | docs/prompts/ | Approved implementation/audit prompts. |

| Manual GitHub upload guidance<br>Create a docs/strategy folder in the repository. Upload the .docx there. Later, if a Markdown version is created, upload it beside the Word document. Do not overwrite without changing the version name or commit message so history stays clear. |
| --- |

# 19. Open Questions and Gaps to Fill

These items need more founder input or beta learning before they can be finalized.

| Question | Current answer | Action |
| --- | --- | --- |
| Who is the single sharpest first contractor persona? | Not finalized; all small contractors, solo-first. | Revisit after beta interviews. |
| Which trade gets the best first templates/tools? | Broad small contractors, with HVAC/plumbing/electrical/handyman/pest/maintenance in scope. | Decide after seeing early user interest. |
| How does Basic vs Premium differ exactly? | $5 Basic Discover/communication, $20 Premium full tools is the current thought. | Define exact feature gates before Stripe. |
| What homeowner features can be monetized? | Storage limits/add-ons possible later. | Do not monetize too early if it slows homeowner growth. |
| How deep should team tools go? | Per-user pricing later; enterprise/team workflow later. | Do not build enterprise product too early. |
| How should beta contractor billing readiness be tracked? | A DB-only contractor billing account and entitlement contract should track beta/free/founder/future status while preserving current free beta access. Platform admins can view read-only readiness summaries; the billing account table is canonical, while legacy contractor profile subscription fields remain compatibility/admin-display data. Contractor dashboard entitlement loading, beta/status labels, and limited read-only UI support are available for future use only. Normal beta/free/full-beta users remain unrestricted, and existing records remain visible. | Do not activate Stripe checkout, charging, payment prompts, paywalls, hidden records/tabs, editable billing controls, backend/RPC enforcement, or production billing enforcement until separately approved. |
| Should general messaging be before or after connection? | Connection request message before; general thread after connection. | Confirm in design phase. |
| Should contractor call tracking be prioritized? | No-priority backlog for calls made/received outside the app. | Revisit only after beta proves a strong workflow need. |

# 20. Immediate Next Steps

1. Treat PRs #1-#11 as the current shipped cleanup baseline for the core MVP loop.
2. Use FB-020 as the umbrella readiness workstream for security, records reliability, backup/restore, storage, dependency security, and scale hardening before broader beta/public go-live.
3. Run a focused beta-readiness/release-checklist audit before starting another major feature.
4. Use the unauthenticated production public smoke check after production deploys, keep authenticated production smoke read-only and limited to dedicated approved smoke accounts, and add optional role credentials or stable smoke records only after separate approval.
5. Use the approved contextual connection request direction in Sections 11 and 13 as the product/spec baseline.
6. Implement contextual connection requests in small reviewed PRs: SQL/RLS/RPC foundation, homeowner request UI without photos, contractor pending request review UI, active connection per-property sharing editor, contractor workspace routing polish, and optional media/photo support later.
7. Do not ask Codex to implement photos/media, general messaging, or new permission categories until those are separately audited and approved.
8. Keep QuickBooks, payments/Stripe, push/email/text automation, native mobile apps, full calendar sync, and contractor call tracking future-facing until the beta loop is stable.

# Appendix A: Contextual Connection Implementation Breakdown

Use this breakdown as the approved product direction for future implementation prompts. It is not implementation approval by itself.

1. Product spec/master plan update.
2. SQL/RLS/RPC foundation for one connection with shared properties and per-property permissions.
3. Homeowner contextual connection request UI without photos.
4. Contractor pending request review UI.
5. Active connection per-property sharing editor.
6. Contractor workspace routing polish for shared properties, open items, and history.
7. Optional media/photo support with storage/RLS tests.

# Appendix B: Draft Codex Audit Prompt for Connection Flow

Use this prompt only when ready to ask Codex for an audit. It is not an implementation approval prompt.

Audit the current ServSync contractor profile, Discover, connection request, permissions, and messaging flow. Do not modify files. Report where the app currently supports: contractor profile CTA, connection request creation, homeowner message/context, property/home context, permission selection, contractor accept/decline, and any general homeowner-contractor messaging outside service requests. Identify minimal frontend, backend, schema, RLS, and notification changes needed to support the first contextual connection request implementation: homeowner clicks Connect, enters a message, selects one or more properties, sets per-property permissions, and sends a request to the contractor. Treat photos/media as a later phase that needs separate storage/RLS design. Also identify whether general connection-level messaging should be built in the same PR or separate PR. Return a risk-rated implementation plan in small PRs. Do not implement.
