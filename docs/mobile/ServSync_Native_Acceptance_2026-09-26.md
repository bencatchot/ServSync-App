# Native Demo rebuild and acceptance — September 26, 2026

This rebuild and focused Back/PDF repair are implemented and validated for review. FB-015 remains **Implementation In Progress** because the platform/integration/device gaps below remain open. The owner approved continuing the mobile status report's next steps; this is not approval for customer distribution, production configuration, a main merge, or an app-store release.

Branch: `codex/mobile-current-acceptance`, starting from `origin/main` at `749d182`, in [PR #582](https://github.com/bencatchot/ServSync-App/pull/582). Final code `f8b60835e042810f1fa9c39b11c93ce1e9e33ec9` is committed and pushed, including the stored-PDF, concurrency, filename, and error-clearing corrections. Final Android replay, hosted Preview, GitHub quality checks, all three Vercel statuses, and post-validation tutorial review passed. The [September 9 matrix](ServSync_Native_Polish_Acceptance_2026-09-09.md) remains historical evidence; older packages do not establish acceptance of this code.

## Build and implementation evidence

- Final code passed mobile bundle/sync and **both clean native builds**. All 52 `dist-mobile` files are byte-identical in the iOS and Android packages; Android contains 54 public files including two empty Cordova stubs.
- Final Android artifact: ignored `mobile-build/servsync-demo-acceptance-debug.apk`, **9,998,156 bytes**, SHA-256 `963b88184abed10beacf94d9a1e94b425f18f3354f92cfaaabacfd4d775152ab`. It installed on the dedicated `ServSync_Pixel_8_API_36`, `emulator-5556`; the installed APK was pulled and its checksum matched. Final stored-PDF runtime acceptance passed as detailed below. This is a development artifact, not a signed store release.
- Final unsigned iOS simulator build installed/launched on the dedicated iPhone simulator (`2821CA81-0355-4FD8-BAF8-532A27C3233A`). Continued CUA acceptance on `f8b60835` verified existing contractor login, Financials/Invoices navigation, sign-out, existing homeowner login, and Documents. All 52 installed public files match `dist-mobile`; cached PDF count is zero. Receipt: ignored `mobile-build/ios-acceptance/followup-results.json`, with a privately retained Homeowner Documents screenshot. No current iOS PDF runtime acceptance is claimed.
- Unsigned physical-iPhone preparation also passed: a generic iOS-device ARM64 build using cached dependencies and `CODE_SIGNING_ALLOWED=NO`, with all 52 bundled web files matching `dist-mobile`. The outer app is unsigned and contains no embedded provisioning profile. Artifact: ignored `mobile-build/ios-device/Build/Products/Debug-iphoneos/App.app`; receipt: `mobile-build/ios-device-evidence.json`; log: `mobile-build/ios-device-build.log`. This is compilation evidence, not an installation or physical-device acceptance result.
- Preserved Back-only Android APK: ignored `mobile-build/servsync-demo-back-only-debug.apk`, SHA-256 `5aec04d8e5fbc012552d66322be4af2d5bb5e840472cd676bfda7bcd4a91a945`. It identifies the earlier Back/composer/session and generated-PDF checks below; final stored-PDF acceptance is tracked separately.
- Earlier Back-only unsigned iOS simulator build passed and installed/launched on the dedicated iPhone simulator (`2821CA81-0355-4FD8-BAF8-532A27C3233A`). That package's UI confirmed restored contractor Business Profile, current Work/At a Glance, Start New Draft, and the existing Estimate. No saved Draft was created by temporary input. Subsequent stored-PDF builds are separate evidence; their build/install/launch does not establish authenticated iOS PDF acceptance. No physical-device signing or provisioning was performed.
- Android Back now detects the current visible modal, including the shared accessible Draft confirmation, before navigating or minimizing. It uses existing dialog cancel/Escape handlers, respects busy states, and preserves component-owned cancellation and focus behavior. Native top-layer dialogs take precedence over background overlays. The change is isolated to `src/mobile/main.ts` and `src/mobile/back.ts`.
- Final mobile suite: **21/21 passed**, including stored-PDF retrieval and native PDF lifecycle/concurrency/filename guards. Real-component browser Back regressions: **6/6 passed**, covering actual Draft cancellation/focus restoration, busy confirmation, active PDF sharing/cleanup, nested dialogs, native top-layer priority, and ordinary history/root behavior. Browser checks isolate the native boundary; they do not replace Android runtime acceptance.
- Final TypeScript and affected-file lint passed; an independent focused review of the concurrency and filename fixes found no remaining findings. Earlier full repository checks passed web build, **27/27 architecture checks**, and lint with **0 errors / 76 existing warnings**; the web build retains its existing chunk-size warning.
- Local logs and screenshots are ignored under `mobile-build/`; credentials, generated bundles, binaries, and signing files are excluded from commits.

## Website Preview verification

Final code `f8b60835e042810f1fa9c39b11c93ce1e9e33ec9` passed authenticated verification on its immutable [Demo Preview](https://servsync-demo-5gsx8lpvs-bencatchots-projects.vercel.app). Homeowner Documents and Home History download entry points returned the same 35,490-byte PDF with unchanged content hashes. Navigation/drawer cancellation and focus restoration passed at 390px; no native class/style leakage, page errors, or horizontal overflow occurred. This verifies the stored-PDF repair preserves those browser downloads. The separate contractor finalized-report caller has no matching Demo fixture and was not exercised at runtime.

Exact code commit `eaed577f2e4b0477cdfb2645eecdeb8d5070c289` passed all three normal Vercel deployment statuses. Its immutable [Demo Preview](https://servsync-demo-ke2t0ueo0-bencatchots-projects.vercel.app) passed **7/7 public-route browser tests** plus authenticated contractor checks at 390px: Work → Start New Draft → blank composer → Back to Work, navigation-drawer Escape dismissal, and focus restoration. No record was saved or sent.

The Preview had no page errors or horizontal overflow. Inspection confirmed a static web body with neither the native class nor native styles; the native Back/viewport presentation is isolated from the website. Authenticated evidence is recorded in local `/tmp/servsync-mobile-preview-results.json`. These web checks supplement, and do not replace, native-device acceptance.

## Current runtime acceptance

Only approved existing fictional Demo accounts and records are used. Temporary composer input is unsaved; no request, estimate, invoice, report, message, payment, signup, or recovery email has been submitted by this acceptance pass.

Final read-only fixture verification preserved the existing one Draft Estimate ($250), one Draft Invoice ($250 / paid $0), zero Jobs, three Documents, and three Home History rows. No test product record was saved or sent.

The continuation checked both approved contractor contexts: each has zero Jobs. All three homeowner report rows have `inspection_id: null`, so they do not provide a legitimate existing fixture for the contractor finalized-report caller. No record was created, reassigned, or modified to manufacture that coverage.

The detailed iPhone composer, keyboard, format-switching, background/resume, section-jump, and chooser checks used this task's current-main rebuild. At the earlier Back-only checkpoint, the package differed only by an Android-only helper; after installing it, contractor-session restoration and current Work/Draft entry were replayed. Start New Draft opened the blank private composer with customer fields, a $0 total, and Save Draft, and the app was left on contractor Dashboard without saving. Later stored-PDF and shared-adapter edits are separate changes. The earlier evidence applies to unaffected iPhone composer/presentation behavior; it does not establish authenticated PDF runtime acceptance on the final package.

| Check | Current evidence | Remaining boundary |
| --- | --- | --- |
| iPhone launch/session | Rebuilt app restored the existing contractor workspace | Physical-device session lifecycle and account isolation still need acceptance |
| Final iPhone login/navigation/role switch | CUA on `f8b60835` verified existing contractor login, Financials/Invoices, sign-out, existing homeowner login, and Documents | Current PDF actions remained outside the reachable viewport; no PDF runtime pass or app scrolling defect is established |
| iPhone current Draft entry | Work → Start New Draft opens the current composer; previous pointer-coordinate scaling issue was resolved | This closes the prior simulator composer-activation blocker only |
| iPhone keyboard/composer | Title, description, and price remain visible with the software keyboard; a temporary line calculates a $125 total | No save or output launch; physical touch/typing remains unverified |
| iPhone format exploration | Standard/checklist round trip preserves the entered title and description | Does not establish persistence after saving or process termination |
| iPhone background/resume | Unsaved line and $125 total remain after background/resume | This is not offline synchronization or durable saved-Draft evidence |
| iPhone typing investigation | One CUA first-line truncation was not reproduced in **16 Chromium/WebKit real-component cases** using the native stylesheet/viewport adapter; DOM identity and focus stayed stable | Do not label this a reproduced app defect or a proven automation-only issue; repeat normal typing on a physical device |
| iPhone scrolling | Earlier keyboard traversal works; continued CUA wheel, pointer drag, and Page Down did not expose the lower PDF actions | Control limitations remain; no app scrolling defect is established, and physical swipe acceptance is open |
| iPhone Business Profile section jump | Passed through CUA pointer input: after hardware Page Up reached the section navigation, Go to section brought Logo & Branding below the header; the Demo label, header, and bottom navigation stayed in position | Visual containment established; no DOM/root-scroll measurement or physical swipe claim |
| iPhone photo/file chooser cancellation | Logo Upload → Photo Library opened the native picker with sample images; Choose File opened native Files Recents. Both canceled cleanly; logo remained empty | No selection, upload, account write, camera capture, or permission grant/denial acceptance |
| Android package identity | User-approved ADB controls and WebView inspection exercised the preserved Back-only package for core UI tests and final `f8b60835` package for the stored-PDF repair. Both installed checksums matched their artifacts | Emulator evidence does not establish physical-device behavior |
| Android Back and confirmation | Actual Back closed the navigation dialog with focus restoration and canceled the accessible unsaved-Draft Create Estimate confirmation; title/line remained and the confirmation's focus was restored | Zero mutation HTTP requests were observed; no saved Draft or Estimate was created |
| Android keyboard/composer | ADB entered the full title and first line; $125 price produced a $125 total. Software keyboard was visible, focused control bottom was 411 CSS px within a 528px viewport, navigation was hidden, and root scroll stayed zero | Physical typing and accessibility remain open |
| Android touch scrolling | Native swipe moved workspace scroll from approximately 1617.9px at the bottom to 1220.2px while root scroll remained zero | Isolated emulator swipe evidence, not physical-device acceptance |
| Android background/resume | Home/background → reopen preserved the unsaved title, line, and $125 total | Does not establish persistence after process termination or offline synchronization |
| Android network loss/recovery | Only this emulator's Wi-Fi/data were temporarily disabled from their original enabled/enabled state. Connection warning appeared, then cleared after both were restored; unsaved input remained | No offline save or synchronization claimed; original connectivity restored |
| Android account switch | Contractor sign-out returned to the public landing; the existing homeowner then signed in to Home History/Documents without contractor controls. Back through an old contractor URL still showed the homeowner role | UI session separation established; this is not a new cross-tenant authorization audit |
| Android history/root/session | Hardware Back traversed history, minimized at history index zero, and reopened with the homeowner session. Force-stop/relaunch also restored Home History | Physical-device lifecycle remains open |
| Android homeowner file chooser | Documents opened the native Files picker; Back canceled with `files.length === 0` | No selection, upload, permission grant/denial, or physical camera acceptance |
| Generated estimate/invoice PDF replay | Both existing Android PDFs rendered correctly as one page through native share → system Print preview: Estimate Draft $250; Invoice Draft $250 / paid $0 / due $250, with expected scope, line items, and totals | Returned without selecting a printer or submitting a print/send. Current iPhone replay remains unverified because the action was below the reachable viewport: CUA drag acted like a click, and wheel/Page Down did not establish scrolling. This is a control limitation, not a reproduced PDF or scrolling app defect |
| Android PDF retry/cancel/cache | For each document, retry reused the same single cached PDF path. Back canceled the share sheet; the next Back closed the Estimate PDF dialog, and Close dismissed the Invoice PDF dialog. Both ended with zero cached PDFs | Existing source documents stayed unchanged; this does not establish every installed viewer or external delivery/save destination |
| Stored report/document opening | Homeowner Home History → View Report and Documents → Download stay in ServSync and open the native PDF dialog. Final `f8b60835` native share → Print replay visually confirmed the correct original three-page water-heater report | Original anchor path launched Chrome's first-run agreement; no agreement was accepted. iOS stored-PDF runtime and the contractor finalized-report caller remain unverified; Demo has zero Jobs for that caller |
| Android stored-PDF retry/cancel/cache | Report retry reused the same single cache path; Back canceled sharing and closed the PDF dialog, while Documents Close also returned the cache to zero. Final `f8b60835` Print replay returned to ServSync and Close left zero cached PDFs; Wi-Fi/data remain enabled/enabled | No printer/send or source-record mutation; not every installed viewer or save destination is covered |
| Android stored-PDF failure/retry | Final `f8b60835` scoped GET failure shows a sanitized screen error without external handoff; retry clears the error and opens the correct PDF | Error behavior and successful retry passed on the final installed package |
| Android duplicate stored-PDF action | With a delayed GET, two actual View Report button clicks on final `f8b60835` produced exactly one PDF GET and one native dialog | Concurrency guard verified in the installed app; no stacked dialog or duplicate cached file |
| Post-validation tutorial freshness | **NONE** after final native and exact-head Preview validation; 26 fresh Production searches and complete TUT-003 revision 1 playback/steps/transcript comparison found no stale guidance | No Help change/publication; native Help transport and the unpublished native PDF guidance draft remain separate follow-ups |

A physical iPhone 16 Pro Max is paired/available with Developer Mode enabled. No physical Android device is connected/authorized; only the dedicated emulator is available. No physical test, signing, provisioning, or installation has been performed. Device availability and unsigned compilation do not establish physical acceptance or authorize signing/configuration changes.

This continuation made no application-code change. Existing source tests and the completed tutorial result remain unchanged. It submitted no product writes, sends, payments, permission changes, or signing/install actions.

The owner subsequently approved alternate Android test controls after CUA could not expose the emulator window. This resumed pass used only the dedicated ServSync emulator and a task-scoped WebView forward on port `9228`; no shared settings were changed. The installed package checksum was verified before acceptance. Temporary composer input was discarded through the normal unsaved-change confirmation; no record was saved. Final cleanup confirmed Wi-Fi/data restored to enabled/enabled and zero cached PDFs; the ADB forward and generated UI XML were removed. Screenshots and receipts are ignored under `mobile-build/android-acceptance/`.

## Stored-PDF repair and final validation

The reproduced Android report-download failure was a presentation/integration gap in the anchor download path. File signing and authorized retrieval succeeded: the Android WebView returned HTTP 200, `application/pdf`, 35,490 bytes, and `%PDF-` for the existing report. The bounded repair routes stored PDFs from the existing homeowner Documents and contractor finalized-report download handlers through `src/utils/storedDocumentDownload.ts` and the existing native PDF adapter. Website behavior and non-PDF anchors remain unchanged; no authentication, storage policy, or CORS changes are needed.

Android replay on the earlier repair establishes in-app report opening, rendering, retry, cancellation, and cleanup. Both entry points clear prior error state before downloading; native failure/retry verified that a successful retry removes the stale banner.

Final review identified two additional bounded defects: concurrent stored-PDF fetches could stack dialogs, and extensionless or long cache filenames could lose PDF type recognition. Both are corrected in `f8b60835`, with **21 mobile checks plus 6 Back regressions**, clean builds, and matched artifact checks passed. Final Android delayed-fetch duplicate-click, failure/retry/error clearing, native rendering, and cleanup all passed. The final hosted Preview preserved the homeowner browser downloads, followed by completed tutorial review. The final iOS build/install/launch passed, but no authenticated iOS stored-PDF runtime result is claimed. The contractor finalized-report caller was updated to use the same tested helper; zero Demo Jobs meant that caller had no runtime fixture.

## Product and integration boundaries

The connected-account request → estimate → approval → job/report → invoice → Home History path uses the existing Supabase client, RPCs, and storage. Representative source paths include `src/App.tsx` request creation, homeowner estimate response, draft saving, estimate/invoice portal sending, accepted-estimate job creation, report finalization, and Home History filing. Native API-server integration is not a prerequisite for exercising those existing connected-account screens. A fresh mutating native lifecycle run is still needed before claiming that the complete loop passed on these packages.

The following remain distinct gaps, not promises delivered by this rebuild:

| Area | Actual source boundary | Required next step |
| --- | --- | --- |
| Stored reports and documents | The homeowner anchor path launched Android Chrome first-run setup. Both existing download handlers now reuse authorized retrieval and the native PDF adapter. Final Android homeowner rendering/retry/error clearing/concurrency/cleanup and unchanged browser downloads passed; filename guards are regression-tested. | Authenticated iOS stored-PDF and the contractor finalized-report caller still need runtime acceptance. No storage access is broadened. |
| Help videos | `src/features/help/helpStudio.ts` calls relative `/api/help-walkthrough-media`. The bundled local origin has no hosted API server, and `server/helpWalkthroughMedia.ts` has no cross-origin/OPTIONS support. | Ordinary end-user Help playback needs an intentional native transport or approved hosted handoff. Written Help availability does not prove video playback. |
| External estimate/report email | Relative `/api/send-local-estimate-email` and `/api/send-finalized-report-email`; server handlers reject foreign Origin headers. | An absolute URL alone is insufficient. Native API/auth/CORS work requires its protected approvals. Demo estimate-email delivery is independently provider-disabled; transport does not enable or authorize sending. |
| Guest document access | Guest estimate/invoice/report routes are relative `/api` calls using secure, HttpOnly, SameSite=Strict web cookies. | Keep generated guest links in their existing hosted web flow for the bounded connected-account Demo. Any in-app guest session requires an explicit cookie/session design. |
| Recovery, invitations, claims, and links | `src/appLinks.ts` deliberately generates hosted Demo links. No JavaScript app-URL/open-launch handler, Android app-link intent filter, or iOS associated-domain/custom-scheme registration is implemented. Native scene forwarding alone does not finish this flow. | Existing-account login can be accepted now; a customer beta needs a deliberate browser-return experience or approved callback/deep-link work. |
| Payments and Marketing providers | Stripe and Facebook endpoints use relative routes and same-origin checks; provider return URLs are web URLs. | Remain excluded from this native increment. Do not broadly relax Origin checks or spoof web origins. |
| Push and offline synchronization | No push integration or synchronization queue is implemented. The current connection notice warns that changes cannot be saved without connectivity. | Optional later scope; temporary in-memory state retention is not offline support. |
| Native session lifecycle | `src/supabaseClient.ts` uses the existing web client without a native secure-storage or explicit foreground/background session adapter. | Review storage and session lifecycle before customer distribution. Successful relaunch/resume does not complete that review. |

## Distribution readiness

Already configured: isolated locally bundled app; exact Demo backend guard; provisional `app.servsync.demo` identity; iOS minimum 15 and Android minimum 24/target 36; filesystem/share/network plugins; iOS camera/photo usage text and filesystem privacy reason; Android backup disabled and FileProvider configured.

Not configured in committed release files: iOS development team/provisioning, Android release signing, store export/release automation, and universal/app-link associations. Both platforms retain version `1.0`, build/version code `1`. Store account access, distribution-certificate readiness, listings, artwork suitability, and complete privacy disclosures were not established by this source audit. No store account or signing secret was inspected. Physical camera/touch, VoiceOver/TalkBack, network/lifecycle behavior, and controlled distribution acceptance remain open.

The continued local readiness check found one valid Apple Development identity through public certificate metadata only; no key material was inspected/exported. No development team/profile has been selected for ServSync, and matching provisioning remains unverified. The unsigned ARM64 artifact above is prepared for review. The next concrete owner gate is authorization to configure ServSync development signing/provisioning with the existing Apple Development identity and install/test the Demo app on the paired iPhone 16 Pro Max. This does not include store distribution or production app/backend configuration.

## Tutorial reconciliation

The old TUT-002 replacement dependency is **resolved**. Production revision **4**, published and verified September 25, demonstrates Work → Start New Draft → Save Draft → Create Estimate with the current starter line, narration, captions, and an unsent result. The publication evidence lives on the separately unmerged recorder-support [PR #577](https://github.com/bencatchot/ServSync-App/pull/577), source `7c45ff6`, in [its acceptance record](https://github.com/bencatchot/ServSync-App/blob/7c45ff6/docs/qa/TUT_002_DEMO_RECORDER_SUPPORT_2026-09-25.md). Publication was an independently approved Help operation; this does not imply the recorder branch has merged.

This resolves the specific stale September 9 estimate-media blocker. The [native PDF guidance draft](../tutorials/NATIVE_PDF_GUIDANCE_DRAFT_2026-09-09.md) remains a draft, not published guidance.

**Tutorial impact: NONE** for the completed Back and stored-PDF increment.

After final native and exact-head Preview validation at `f8b60835`, 26 fresh Production queries across homeowner/contractor roles and Documents, Home History, report, PDF, download, and native terms/contexts found only TUT-003, **How to complete work and save the service record**, revision 1. Secure media returned HTTP 200; full normal-speed playback ended at 57.32/57.32 seconds with no error. Written steps/transcript and four sampled frames still show Complete Job → Finalize Report → Filed to Documents/Home History accurately. The lesson does not demonstrate native PDF opening/sharing. Review used muted playback plus transcript/frame comparison, not renewed audio/caption certification. No Help record, media, or publication changed.

Final local evidence: `/tmp/servsync-stored-pdf-production-tutorial-search.json`, `/tmp/servsync-stored-pdf-production-tutorial-playback.json`, and `/tmp/servsync-stored-pdf-production-tutorial-frames/`.

The earlier Back-only application/Preview review also returned NONE. Its 22 fresh searches in each of Demo and Production covered the affected roles, `contractor.drafts`/`contractor.financials` contexts, Draft/Create Estimate/confirmation terms, and native/mobile/Android/Back/cancel/PDF/share terms. Demo returned no published matches; Production supplied the authoritative published-library comparison. Native-specific terms matched no lessons. Broader matches were TUT-001 revision 1, TUT-002 revision 4, TUT-003 revision 1, and TUT-004 revision 1.

All four matching lessons' written steps/transcripts and secure videos were reviewed to the end at normal speed, with sampled visual frames. TUT-002 shows the current Start New Draft, open first line, Next step Estimate, Save Draft/Create Estimate confirmation, and final unsent Draft. TUT-001 retains request-to-estimate entry, TUT-003 ends Filed to Documents, and TUT-004 retains the outside-payment modal and Partially Paid outcome. The native handler reuses existing cancellation behavior without changing those web paths, labels, or outcomes; no Android Back instruction is contradicted. Playback was muted with visual/transcript comparison, not a renewed audio/caption-quality certification. No Help publication or record mutation occurred.

Local evidence: `/tmp/servsync-mobile-production-tutorial-search.json`, `/tmp/servsync-mobile-production-tutorial-playback.json`, and `/tmp/servsync-mobile-production-tutorial-frames/`. Hosted playback does not prove bundled-native Help video transport.

## Next acceptance boundary

The focused implementation is ready for owner review in PR #582. FB-015 remains **Implementation In Progress**. The immediate owner decision is the bounded development signing/provisioning and paired-iPhone installation/testing authorization described above; the unsigned device build is already prepared. Complete current iOS PDF and physical touch/camera/accessibility/session checks, and retain the contractor caller gap until a legitimate fixture is available. A distribution-focused increment must also define native Help/API transport, callback handling, secure-session review, and distribution with the exact required configuration approvals. Marketing providers, online payments, push, and offline synchronization can remain explicitly excluded from a controlled first device beta.

No schema, RLS/RPC, storage-policy, auth/provider/environment setting, production record, main merge, production deployment, or store release is part of this increment. The master plan's product direction is unchanged. The marketing inventory was reviewed; its existing native-app/store/offline claim limits remain accurate, so no inventory edit is needed.
